---
phase: 05.1-add-reconnect-progress-bar-fix-version-mismatch-toast-wiring
plan: 01
subsystem: frontend-connection-ux
tags: [react-ui, toast, gap-closure]

requires:
  - phase: 05-frontend-polish
    provides: useConnectionStore (apps/web/src/store/connection.ts), ToastQueue error-toast pipeline
provides:
  - Local-client "Reconnecting... (Ns)" progress bar, driven by real connection-status state
  - Removal of the unreachable/fixture-only version-mismatch toast case that had no real VERSION_MISMATCH error code behind it
affects: [05-frontend-polish]

actuals:
  tokens: null
  tasks: 1
  commits: 1

tech-stack:
  added: []
  patterns:
    - "ReconnectBanner tracks 'has this client ever been open' via a ref, so it never shows on the FIRST connect (only on a drop after a prior successful open) — mirrors the connecting/open/closed lifecycle in useConnectionStore"
    - "sessionTakenOver prop suppresses the banner so it doesn't compete with the existing session-taken-over banner for the same visual space"

key-files:
  created:
    - apps/web/src/components/ReconnectBanner.tsx
    - apps/web/src/__tests__/ReconnectBanner.test.tsx
  modified:
    - apps/web/src/App.tsx
    - apps/web/src/__tests__/ToastQueue.test.tsx
    - .planning/phases/05-frontend-polish/05-04-SUMMARY.md

key-decisions:
  - "Removed the version-mismatch toast case rather than building a real VERSION_MISMATCH server-to-client error path: this project has no independently-versioned client/server release process (single Bun-workspace monorepo, deployed together), so there is no real version-skew scenario to guard against. The prior ToastQueue.test.tsx assertion was fixture-only (called addToast() directly with a hardcoded string, never exercising a production error path) and 05-04-SUMMARY.md's 4-case claim overstated what ROADMAP.md Success Criterion 4 actually requires (2 cases, both already correctly wired)."
  - "Initial agy delegation hit a model-capacity error (503) partway through and left ReconnectBanner.tsx written but unmounted, with no test file. Claude finished the task directly: mounted <ReconnectBanner sessionTakenOver={sessionTakenOver} /> in App.tsx (positioned before GraceBanner, after the session-taken-over banner) and wrote the missing test file."

patterns-established: []

requirements-completed: [REQ-06]

coverage:
  - id: D1
    description: "A local-client reconnect progress indicator is visible while the client's own connection is attempting to recover, driven by real connection-status transitions (not shown on first connect, hidden again on reopen or session takeover)"
    requirement: "REQ-06"
    verification:
      - kind: unit
        ref: "apps/web/src/__tests__/ReconnectBanner.test.tsx"
        status: pass
    human_judgment: false
  - id: D2
    description: "No toast/test asserts an unreachable production error path"
    requirement: "REQ-06"
    verification:
      - kind: unit
        ref: "apps/web/src/__tests__/ToastQueue.test.tsx"
        status: pass
    human_judgment: false

duration: null
completed: 2026-09-16
status: complete
---

# Phase 05.1 Plan 01: Add reconnect progress bar, fix version-mismatch toast wiring Summary

**Added a local-client "Reconnecting... (Ns)" progress bar driven by real connection-status state, and removed the unreachable version-mismatch toast case that a prior SUMMARY had overclaimed as wired.**

## Accomplishments
- `apps/web/src/components/ReconnectBanner.tsx` (new): renders only after a connection has been open at least once and then drops (never on first connect), counts elapsed seconds, scales a progress bar against a 60s reference, suppressed by a `sessionTakenOver` prop.
- Mounted in `App.tsx` alongside the existing session-taken-over banner and `GraceBanner`.
- `ToastQueue.test.tsx` corrected: removed the fixture-only version-mismatch assertion, realigned the "Server Restart" body text to what `App.tsx` actually emits.
- `05-04-SUMMARY.md` corrected: removed "Version Mismatch" from its copywriting-contract claim.
- New `ReconnectBanner.test.tsx` (6 tests): covers not-yet-open, open, drop-after-open, elapsed-seconds counting, hide-on-reopen, and sessionTakenOver suppression.
- Full verify chain green: typecheck clean, 154/154 backend tests, 85/85 web tests (79 prior + 6 new).

## Task Commits

1. **Add ReconnectBanner + fix version-mismatch toast overclaim** - (this commit)

## Files Created/Modified
- `apps/web/src/components/ReconnectBanner.tsx` - new component (agy-authored, Claude-mounted)
- `apps/web/src/__tests__/ReconnectBanner.test.tsx` - new test file (Claude-authored, agy's delegation was interrupted by a model-capacity error before writing tests)
- `apps/web/src/App.tsx` - mounts `<ReconnectBanner sessionTakenOver={sessionTakenOver} />`
- `apps/web/src/__tests__/ToastQueue.test.tsx` - removed unreachable version-mismatch assertion, realigned server-restart copy
- `.planning/phases/05-frontend-polish/05-04-SUMMARY.md` - corrected copywriting-contract claim

## Decisions Made
- Chose to remove the unwired version-mismatch toast rather than build real version-skew detection infrastructure — no real skew risk exists in this project's single-monorepo deploy model. See key-decisions above for full rationale.

## Deviations from Plan
- The agy delegation for this phase failed partway through (Gemini API 503 capacity error) after writing `ReconnectBanner.tsx` but before mounting it or writing tests. Claude completed the remaining work directly (mount + test file) rather than re-delegating into an ambiguous-provenance working tree.

## Issues Encountered
- Delegated concurrently with 03.1/04.1/06.1 in a shared working tree; this delegation's own agy instance additionally reported (without authoring) several files that actually belonged to sibling phases — confirmed via its own conversation-log check that it did not write drain-latch or word-correctness code. Independently verified the final on-disk state is correct and non-conflicting.

## Next Phase Readiness
- No blockers. Ready for re-verification.

---
*Phase: 05.1-add-reconnect-progress-bar-fix-version-mismatch-toast-wiring*
*Completed: 2026-09-16*
