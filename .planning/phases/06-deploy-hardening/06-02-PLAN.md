---
phase: 06-deploy-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .bun-version
  - packages/shared/src/__tests__/bun-version-pin.test.ts
autonomous: true
requirements: [REQ-12]

estimate:
  tokens: 10000
  raw_tokens: 7500
  tasks: 1
  confidence: low

must_haves:
  truths:
    - ".bun-version at repo root pins the local dev Bun toolchain to exactly 1.3.2 (D-04)."
    - "package.json's engines.bun range (>=1.3.2 <1.5.0) and packageManager (bun@1.3.2) are unchanged — not tightened (D-04)."
    - "All 4 Dockerfiles (root, apps/web, apps/gateway, apps/engine) already build with the exact pinned Bun version via `ARG BUN_VERSION=1.3.2` — verified true at plan-authoring time (git ls-files + grep), now regression-guarded so future drift fails a fast local test instead of going unnoticed (D-05)."
  artifacts:
    - .bun-version
    - packages/shared/src/__tests__/bun-version-pin.test.ts
  key_links:
    - ".bun-version content <-> each of the 4 Dockerfiles' `ARG BUN_VERSION` default <-> package.json's `packageManager` field, cross-checked by the new regression test"
---

<objective>
Add the missing `.bun-version` local-dev pin (D-04) and add a regression test that guards the Bun-version pin across `.bun-version`, all 4 Dockerfiles, and `package.json` from silently drifting apart.

Purpose: 06-CONTEXT.md's D-05 assumed all 4 Dockerfiles still needed pinning — they don't. `git ls-files` + inspection confirms `Dockerfile`, `apps/web/Dockerfile`, `apps/gateway/Dockerfile`, and `apps/engine/Dockerfile` already declare `ARG BUN_VERSION=1.3.2` and consume it as `FROM oven/bun:${BUN_VERSION}-slim`. The only genuinely missing D-04/D-05 artifact is `.bun-version` itself (confirmed absent at repo root). This plan closes that real gap and adds regression coverage so the already-correct Dockerfile state stays correct, rather than silently re-doing already-done work.

Output: `.bun-version` (content `1.3.2`), and a fast `bun test` regression guard for the whole pin surface.
</objective>

<execution_context>
@/home/nacs/.claude/gsd-core/workflows/execute-plan.md
@/home/nacs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-deploy-hardening/06-CONTEXT.md
@package.json
@Dockerfile
@apps/web/Dockerfile
@apps/gateway/Dockerfile
@apps/engine/Dockerfile
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: .bun-version pin + Dockerfile/package.json drift guard</name>
  <files>.bun-version, packages/shared/src/__tests__/bun-version-pin.test.ts</files>
  <behavior>
    - `.bun-version` exists at repo root and its trimmed content is exactly `"1.3.2"`.
    - Each of `Dockerfile`, `apps/web/Dockerfile`, `apps/gateway/Dockerfile`, `apps/engine/Dockerfile` contains the line `ARG BUN_VERSION=1.3.2` (read via `Bun.file(path).text()`, matched with a regex, not a naive substring check that would also match a stray comment).
    - `package.json`'s `engines.bun` field is still exactly `">=1.3.2 <1.5.0"` and `packageManager` is still exactly `"bun@1.3.2"` — proving D-04's "engines.bun stays as the existing range, not tightened" holds.
  </behavior>
  <action>
    Create `.bun-version` at the repo root containing `1.3.2` followed by a single trailing newline (matches the version already pinned in Phase 1 Plan 01's `packageManager` field and every Dockerfile's `ARG BUN_VERSION` default — no other value is consistent with the existing tree).

    Create `packages/shared/src/__tests__/bun-version-pin.test.ts` implementing the behavior above. Read all file paths relative to `process.cwd()` (bun test's cwd is the repo root when invoked via `bun test packages/shared`, matching this project's root `test` script), using `Bun.file(...).text()` for each of `.bun-version`, the 4 Dockerfiles, and `package.json` (parsed with `JSON.parse` for the `engines.bun`/`packageManager` assertions, regex-matched for the Dockerfile `ARG BUN_VERSION=` lines). This is a regression guard, not new production behavior — the Dockerfile assertions are expected to pass immediately (D-05 is already satisfied); only the `.bun-version` assertion requires this task's file creation to pass.
  </action>
  <verify>
    <automated>bun test packages/shared/src/__tests__/bun-version-pin.test.ts</automated>
    <fails_when>non-zero exit code, or "fail" appears in the test summary line</fails_when>
  </verify>
  <done>`.bun-version` exists with content `1.3.2`; `bun test packages/shared/src/__tests__/bun-version-pin.test.ts` passes, proving all 4 Dockerfiles and package.json already match it and will keep matching it on every future `bun run --filter '*' test`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dockerfile `ARG BUN_VERSION` -> `oven/bun:${BUN_VERSION}-slim` base image tag | Docker Hub tag mutability boundary — tags are not immutable references |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06 | Tampering | Dockerfile x4 (`FROM oven/bun:${BUN_VERSION}-slim`) | medium | accept | Tag-based pinning (not digest-pinning) means `oven/bun:1.3.2-slim` could theoretically be repointed upstream by the image maintainer. D-05 explicitly chose exact-version-tag pinning as sufficient reproducibility for this phase; digest-pinning (`@sha256:...`) is a stronger but unrequested future hardening, not authorized by any D-NN in 06-CONTEXT.md. |
| T-06-07 | Tampering | `.bun-version` / Dockerfile / package.json drift | low | mitigate | The new regression test (this plan's Task 1) fails the moment `.bun-version` and any Dockerfile's `ARG BUN_VERSION` default (or package.json's `engines.bun`/`packageManager`) diverge, catching accidental drift on the next `bun test` run. |

No new npm/pip/cargo installs in this plan — the package-legitimacy gate is not applicable (no `T-06-SC` row).
</threat_model>

<verification>
- `bun test packages/shared/src/__tests__/bun-version-pin.test.ts` passes.
- `bun run --filter '*' typecheck` unaffected (no source files touched, only a new test file and a plain-text version file).
</verification>

<success_criteria>
- `.bun-version` exists and reads `1.3.2`.
- A committed regression test proves the Bun-version pin is consistent across `.bun-version`, all 4 Dockerfiles, and `package.json` — matching ROADMAP Phase 6's Bun-pinning intent (D-04, D-05).
</success_criteria>

<output>
Create `.planning/phases/06-deploy-hardening/06-02-SUMMARY.md` when done
</output>
