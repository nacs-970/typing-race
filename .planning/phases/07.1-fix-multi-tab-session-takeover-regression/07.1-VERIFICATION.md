---
phase: 07.1-fix-multi-tab-session-takeover-regression
verified: 2026-09-16T15:40:00Z
status: human_needed
score: 2/2 must-haves verified
covered_files:
  - .planning/PROJECT.md
  - .planning/phases/07.1-fix-multi-tab-session-takeover-regression/07.1-01-PLAN.md
  - .planning/phases/07.1-fix-multi-tab-session-takeover-regression/07.1-01-SUMMARY.md
  - .planning/phases/07.1-fix-multi-tab-session-takeover-regression/07.1-CONTEXT.md
  - .planning/v1.0-MILESTONE-AUDIT.md
  - apps/engine/src/__tests__/reconnect.test.ts
  - apps/engine/src/rooms/manager.ts
covered_digest: "v1:sha256:2b0962454d2fe3a1110e964edc59004a1f1f579ee0a351465cb91d2a4e25f4f5"
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "Join a room in browser tab A. Open the same room URL (same sessionToken, e.g. via localStorage/hash) in tab B in the same browser and rejoin the session."
    expected: "Tab A's WebSocket closes with code 1000 and reason \"Session taken over by another tab\"; tab A shows the \"session active in another tab\" banner (sessionTakenOver state) with a working reclaim action; other players in the room do NOT see a spurious \"opponent disconnected\" toast/message from tab A's closed socket."
    why_human: "Tests 4-5 call RoomManager.rejoinPlayer() directly in-process and assert bridge events landed on an InMemoryEventBridge — they prove the engine tier publishes the two events, not that a real socket closes, or that apps/web renders the banner and suppresses reconnect. The gateway relay (ws.send/ws.close in handlers.ts) and the web client's consumer (race-client.ts, App.tsx) were confirmed only by direct code read, not by an automated test driving two real sockets through the full stack. The phase goal itself is phrased as a browser-tab flow (\"opening a second tab... must notify and evict the first tab's socket\"), which is real-time, cross-process behavior outside what a single-process unit test can prove."
---

# Phase 07.1: Fix Multi-Tab Session Takeover Regression Verification Report

**Phase Goal:** Restore the multi-tab session-takeover notify-then-evict behavior (a
working Phase 4 deliverable) that was silently dropped during the Phase 7
monolith-to-N-tier split — opening a second tab on the same session must notify
and evict the first tab's socket, matching pre-Phase-7 behavior.
**Verified:** 2026-09-16T15:40:00Z
**Status:** human_needed
**Re-verification:** No — initial verification (only 07.1-01-SUMMARY.md existed;
no prior VERIFICATION.md). This verification independently re-derives the same
conclusion `.planning/v1.0-MILESTONE-AUDIT.md`'s Integration Check section
reached via `gsd-integration-checker` + agy cross-model read — by reading the
code and running the tests myself, not by trusting either prior write-up.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Opening a second browser tab and rejoining with the same `sessionToken` evicts the first tab's socket with a `session_taken_over` notice, matching pre-Phase-7 behavior | ✓ VERIFIED | `apps/engine/src/rooms/manager.ts:230-262` — `isMultiTabTakeover = oldPlayerId !== newPlayerId` gates two `bridge.publishToGateway()` calls (`send_to_client{session_taken_over}` then `disconnect_client{code:1000, reason:"Session taken over by another tab"}`) targeting `oldPlayerId`, placed after `await this.store.set(...)`. Behaviorally proven at the engine tier (the tier this plan scoped the fix to — D-04 explicitly declared gateway/`packages/shared` already correct) by named test `reconnect.test.ts#4` — ran directly, **PASS**. The cross-process browser-tab flow (real socket close, real banner render) is not exercised by any automated test — see Human Verification Required below. |
| 2 | A duplicate/retried `rejoin_room` on the same still-open connection (resolving to its own currently-assigned `playerId`) does not trigger a spurious `session_taken_over` notice or `disconnect_client` self-eviction | ✓ VERIFIED | Same gating boolean is `false` when `oldPlayerId === newPlayerId`, so neither publish fires; existing `rejoined_room`/`player_reconnected` events are untouched (unconditional, outside the `if`). Behaviorally proven by named test `reconnect.test.ts#5` — ran directly, **PASS**. |

**Score:** 2/2 truths verified (0 present, behavior-unverified)

Both truths are state-transition/notify-evict invariants (Step 3 "behavior-dependent
truths"). Presence/wiring alone would not have been sufficient — both are VERIFIED
specifically because I ran the two named tests myself (not merely counted them)
and they passed against the actual `RoomManager` code, not a mock. They are scored
VERIFIED (not `PRESENT_BEHAVIOR_UNVERIFIED`) because the plan explicitly scoped
the engine tier as the fix surface and that tier's transition is proven end-to-end
by a real test against real bridge-event capture. The *cross-tier browser flow*
implied by the ROADMAP goal wording is a separate, additional concern — captured
as a human-verification item, not a truth downgrade.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `apps/engine/src/rooms/manager.ts` | `rejoinPlayer()` publishes notify+evict on multi-tab takeover | ✓ VERIFIED | Exists, substantive (19 lines added, real logic not a stub), wired — `rejoinPlayer()` is called from `apps/engine/src/engine.ts` on `rejoin_room` client messages and exercised end-to-end by the reconnect test suite. |
| `apps/engine/src/__tests__/reconnect.test.ts` | Regression tests locking in both paths | ✓ VERIFIED | Exists, substantive — tests 4 and 5 added with real assertions against `gatewayEvents` captured from the real `InMemoryEventBridge`, not hand-rolled expectations. Both pass. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `RoomManager.rejoinPlayer()` | `apps/gateway/src/ws/handlers.ts` `send_to_client` case | `bridge.publishToGateway({type:'send_to_client', payload:{type:'session_taken_over'}})` | ✓ WIRED (code read; not driven by an automated real-socket test) | `handlers.ts:57-66` — `case "send_to_client"`: looks up the socket via `manager.getSocket(event.playerId)` and calls `ws.send(JSON.stringify(event.payload))`. `event.playerId` is `oldPlayerId`, so the frame reaches the *old* tab's socket, not the new one. |
| `RoomManager.rejoinPlayer()` | `apps/gateway/src/ws/handlers.ts` `disconnect_client` case | `bridge.publishToGateway({type:'disconnect_client', code:1000, reason:'Session taken over by another tab'})` | ✓ WIRED (code read; not driven by an automated real-socket test) | `handlers.ts:68-76` — `case "disconnect_client"`: looks up the socket and calls `ws.close(event.code ?? 1000, event.reason ?? ...)`, i.e. `ws.close(1000, "Session taken over by another tab")` on the old tab's socket. |
| Old socket `close` | Engine `handlePlayerDisconnect` | `apps/gateway/src/ws/handlers.ts:148-155` (`close` handler) publishes `client_disconnected` for `oldPlayerId` → `apps/engine/src/engine.ts:106-113` routes to `RoomManager.handlePlayerDisconnect(oldPlayerId, ...)` | ✓ WIRED, no phantom broadcast (code read) | `handlePlayerDisconnect` (`manager.ts:183-192`) does `room.players.get(playerId)`; since `oldPlayerId` was already `.delete()`d from `room.players` synchronously inside `rejoinPlayer()` before the socket ever closes, `player` is `undefined` and the function returns early — no `player_disconnected` broadcast leaks to the other players in the room from the evicted-socket's own close event. Confirmed by direct code read, matching the audit's step 8 claim; folded into the human-verification item above rather than left as a silent gap, since no automated test exercises this specific sub-case either. |
| `session_taken_over` frame | Client consumer | `apps/web/src/net/race-client.ts:308-311` and `apps/web/src/App.tsx:117,229-238` | ✓ WIRED (pre-existing, now reachable; code read only) | Client already handles `msg.type === "session_taken_over"` by suppressing auto-reconnect and showing a "session active in another tab" banner — this consumer code pre-dates the fix and was previously dead code because the server never emitted the frame; it is now genuinely reachable. No automated test asserts the banner renders in response to a real server-sent frame. |

### Requirements Coverage

| Requirement | Source | Description | Status | Evidence |
|-------------|--------|--------------|--------|----------|
| REQ-07 | `.planning/PROJECT.md` line 32 (7th Active requirement) / `07.1-01-PLAN.md` frontmatter | "Reconnect mid-race without corrupting race state" (multi-tab takeover sub-behavior) | ✓ SATISFIED at the engine tier; cross-tier browser flow pending human verification | Single-tab reconnect path already covered by Phase 4 / `reconnect.test.ts` test 3. Multi-tab takeover path (this phase) closes the engine-tier gap found by `/gsd-audit-milestone` (REQ-07-multitab finding in `.planning/v1.0-MILESTONE-AUDIT.md`) — tests 4 and 5 lock in both the fix and the no-regression case. |

No orphaned requirements: `.planning/PROJECT.md` maps no other requirement to this
phase, and the phase's own `requirements: [REQ-07]` frontmatter matches.

### Anti-Patterns Found

None. Scanned both modified files (`apps/engine/src/rooms/manager.ts`,
`apps/engine/src/__tests__/reconnect.test.ts`) for `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER`
and `placeholder|coming soon|not yet implemented` — zero matches. No empty-implementation
patterns (`return null`/`return {}`/`=> {}`) introduced by this diff — the two
`git show` diffs (`ac021f4`, `d3c9a28`) are additive only, as the plan's
`<reversibility>` note claims.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Multi-tab takeover test (truth 1) + duplicate-rejoin no-op test (truth 2) | `bun test apps/engine/src/__tests__/reconnect.test.ts apps/engine/src/__tests__/rooms.test.ts` | `13 pass, 0 fail, 52 expect() calls` | ✓ PASS |
| Full engine/gateway/shared regression + typecheck (no collateral break) | `bun run typecheck && bun test packages/shared apps/gateway apps/engine` | `tsc --noEmit` clean across all 4 workspaces (`packages/shared`, `apps/web`, `apps/gateway`, `apps/engine`); `150 pass, 0 fail, 121417 expect() calls across 23 files` | ✓ PASS |
| Web workspace test suite (declared in PLAN's `<success_criteria>` full-chain command; previously only taken on SUMMARY's word) | `bun run --cwd apps/web test` | `Test Files 12 passed (12); Tests 78 passed (78)` | ✓ PASS |

Ran once each, from the repo root, no server started, no state mutated beyond the
test runner's own in-memory fixtures. All three commands declared in the plan's
`<success_criteria>` full verify chain were independently re-run by the verifier
(not taken on SUMMARY's word) and match its claimed counts exactly.

### Probe Execution

No probes apply — this project has no `scripts/*/tests/probe-*.sh` files and
neither the PLAN nor SUMMARY declares any probe-based verification for this
phase (its own `<verify><automated>` blocks are plain `bun test`/`bun run
typecheck` commands, all executed above).

### Human Verification Required

### 1. Real two-tab session takeover in the browser

**Test:** Join a room in browser tab A. Open the same room URL (same
`sessionToken`) in tab B in the same browser and rejoin the session.
**Expected:** Tab A's WebSocket closes with code 1000 and reason "Session
taken over by another tab"; tab A shows the "session active in another tab"
banner with a working reclaim action; other players in the room do not see a
spurious "opponent disconnected" message triggered by tab A's closed socket.
**Why human:** Tests 4-5 call `RoomManager.rejoinPlayer()` directly in-process
and assert on an `InMemoryEventBridge` — they prove the engine tier publishes
the two events correctly, not that a real socket closes end-to-end through
the gateway, or that the web client actually renders the banner and
suppresses reconnect on a real server-sent frame. The gateway relay
(`ws.send`/`ws.close`) and the web consumer were confirmed by direct code
read only, not by an automated test driving two real sockets through the full
stack. The ROADMAP goal itself is phrased as a browser-tab flow, which is
real-time cross-process behavior a single-process unit test cannot prove.

### Gaps Summary

No gaps. The engine-tier fix is behaviorally proven correct by real,
independently-re-run tests, and the full declared verify chain (typecheck +
150 engine/gateway/shared tests + 78 web tests) passes with zero failures —
matching the SUMMARY's claims exactly rather than merely trusting them. This
phase routes to `human_needed`, not `gaps_found`: nothing is missing, stubbed,
or unwired — the one open item is that the cross-process browser-tab flow
(real socket close + real banner render) has only been confirmed by code
read, not exercised by an automated or manually-observed end-to-end run.
Recommend a quick manual two-tab check (see Human Verification Required
above) before treating REQ-07-multitab as fully closed for the v1.0
milestone.

---

_Verified: 2026-09-16T15:40:00Z_
_Verifier: Claude (gsd-verifier)_
