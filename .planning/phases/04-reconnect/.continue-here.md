---
context: phase
phase: 04-reconnect
plan: 1
task: 1
total_tasks: 4
status: ready_to_execute
last_updated: "2026-09-02T15:11:00.000Z"
---

<current_state>
Phase 4 (Reconnect & Room Lifecycle) discussion and planning are complete.
All implementation decisions have been locked into `.planning/phases/04-reconnect/04-CONTEXT.md` and plans `04-01-PLAN.md` through `04-04-PLAN.md` (committed in `85638c9`).
Ready to execute Plan 04-01 (sessionToken issuance & rejoin_room handshake).
</current_state>

<completed_work>
- Phase 3 (Race Track + WPM): 100% complete, all 5 UAT tests passed (commit `c45a1c1`).
- Phase 4 Discussion: Completed via `gsd-discuss-phase 4`.
  - User decided to use browser cookies (`typing_race_${roomCode}=${sessionToken}`) for session persistence.
  - User decided newest tab takes over existing session seamlessly; older tab gets `session_taken_over` notice.
  - User decided race continues live during disconnect; opponents see 60s countdown toast while typing uninterrupted.
  - User decided rejoining player gets instant UI restore with 500ms anti-burst protection.
  - User explicitly excluded the idle room sweeper (no idle timeout). Rooms remain alive until empty.
- Phase 4 Plans: `04-01-PLAN.md`, `04-02-PLAN.md`, `04-03-PLAN.md`, `04-04-PLAN.md` updated and committed (`85638c9`).
</completed_work>

<remaining_work>
- Plan 04-01: sessionToken issuance & rejoin_room wire schemas, server session mapping, and re-binding handshake.
- Plan 04-02: rejoined_room snapshot frame, cookie persistence, multi-tab takeover, and 500ms anti-cheat grace period.
- Plan 04-03: WS heartbeat ping/pong (15s/5s) and per-IP room creation rate limiting (10/hr).
- Plan 04-04: Disconnect UX (60s grace period, player_disconnected / player_reconnected broadcasts, opponent notification toasts).
</remaining_work>

<decisions_made>
- D-01: Session token stored in browser cookie (`typing_race_${roomCode}=${sessionToken}; path=/; SameSite=Lax`).
- D-02: Multi-tab takeover — opening new tab re-binds WebSocket to existing Player slot; old tab gets disconnected.
- D-03: Reconnect wire schema `{ type: "rejoin_room", roomCode, sessionToken }`.
- D-04: Live race flow — opponents continue typing uninterrupted when someone disconnects.
- D-05: 60s disconnect grace period before permanent player eviction.
- D-06: Instant UI restore on reconnect with 500ms server anti-burst guard.
- D-07: Exclude idle room sweeper (rooms stay alive until empty).
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/phases/04-reconnect/04-CONTEXT.md` — Locked decisions and domain scope
2. `.planning/phases/04-reconnect/04-01-PLAN.md` — Immediate next plan to execute
3. `packages/shared/src/messages.ts` — Wire schema single source of truth

## Infrastructure State
- Server: Bun dev server running at `http://localhost:8080` (task `task-640`).
- Web client: Vite dev server running at `http://localhost:5173` (task `task-241`).
- Tests: 71 server tests + 9 web tests passing cleanly.
