---
phase: 03-race-track
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - apps/server/src/race/scoring.ts
  - apps/server/src/race/validate-keystroke.ts
  - apps/server/src/__tests__/scoring.test.ts
  - apps/server/src/__tests__/validate-keystroke.test.ts
autonomous: true
requirements: [REQ-05]
user_setup: []

estimate:
  tokens: 36000
  raw_tokens: 24000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "computeNetWpm matches D-05 formula verbatim: max(0, (correctChars/5) - (uncorrectedErrors/5)) / minutesElapsed"
    - "computeAccuracy matches D-06 formula verbatim: correctChars / totalKeystrokes (0 when totalKeystrokes === 0)"
    - "Spec fixture verified: 30 correct chars / 30s / 0 errors = 12 WPM (A1 discrepancy from ROADMAP 2 WPM surfaced in plan + test comment)"
    - "Spec fixture verified: 0 correct chars / 60s / 0 errors = 0 WPM (not negative)"
    - "Spec fixture verified: 5 correct chars / 5s / 10 uncorrected errors → 0 WPM (clamped at zero, not negative)"
    - "validateKeystroke calls computeNetWpm on each accept with elapsed = now - room.startsAtServerMs (D-05)"
    - "Player.currentWpm is updated to the real formula on every accepted keystroke (Plan 02 had placeholder 0)"
  artifacts:
    - apps/server/src/race/scoring.ts — gains computeNetWpm + computeAccuracy pure functions
    - apps/server/src/__tests__/scoring.test.ts (NEW) — 8+ fixtures covering D-05/D-06 spec, edge cases, and ROADMAP-vs-D-05 discrepancy note
  key_links:
    - "validateKeystroke calls computeNetWpm({ correctChars: countCorrectChars(newStates), uncorrectedErrors, elapsedMs: now - room.startsAtServerMs }) and stamps player.currentWpm (Plan 02 placeholder → real formula)"
    - "Plan 04 race-end broadcast uses computeAccuracy(player.charStates, player.totalKeystrokes) to populate results[].accuracy"
---

<objective>
Wire the server-authoritative WPM and accuracy formulas from D-05/D-06 as pure functions in scoring.ts, call them from validateKeystroke on each accept (replacing Plan 02's placeholder 0), and lock the spec fixture with unit tests. Surface the ROADMAP-vs-D-05 unit discrepancy in the test fixture so future readers know it's intentional.

Purpose: D-05 (live net WPM via cursor_update), D-06 (results-board accuracy), D-07 (no raw WPM live). Plan 04 race-end broadcast reads player.currentWpm + a one-shot computeAccuracy(player) call to populate results[].accuracy.

Output: Two new pure functions (computeNetWpm + computeAccuracy) in scoring.ts, validateKeystroke call site updated to compute and stamp real WPM, 8+ fixture tests in scoring.test.ts including the 30-chars/30s = 12 WPM spec fixture with a discrepancy note.
</objective>

<execution_context>
@/home/nacs/Documents/git/typing-race/.hermes/gsd-core/workflows/execute-plan.md
@/home/nacs/Documents/git/typing-race/.hermes/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-race-track/03-CONTEXT.md
@.planning/phases/03-race-track/03-RESEARCH.md
@.planning/phases/03-race-track/03-VALIDATION.md
@.planning/phases/02-race-engine/02-04-SUMMARY.md
@apps/server/src/race/scoring.ts
@apps/server/src/race/validate-keystroke.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: computeNetWpm + computeAccuracy pure functions with spec fixtures</name>
  <files>apps/server/src/race/scoring.ts, apps/server/src/__tests__/scoring.test.ts</files>
  <action>
Tracer: prove the WPM and accuracy formulas are correct against D-05/D-06 spec before wiring them into validateKeystroke. E2 in Nyquist validation map.

Per RESEARCH.md "Don't Hand-Roll" (WPM formula is a contract; backspace correctness is a data shape) + D-05/D-06 verbatim + A1 ROADMAP-vs-D-05 discrepancy:

1. Extend apps/server/src/race/scoring.ts — add two new pure functions:
   - computeNetWpm(args: { correctChars: number; uncorrectedErrors: number; elapsedMs: number }): number
     - Per D-05 verbatim: const minutes = elapsedMs / 60_000; if (minutes <= 0) return 0; const raw = (correctChars / 5 - uncorrectedErrors / 5) / minutes; return Math.max(0, raw);
     - IMPORTANT: code D-05 verbatim — do NOT rewrite as (correctChars - uncorrectedErrors) / 5 / minutes (RESEARCH.md Pitfall 2). The Math.max(0, …) clamp on the raw intermediate is part of the spec.
   - computeAccuracy(args: { correctChars: number; totalKeystrokes: number }): number
     - Per D-06 verbatim: if (totalKeystrokes === 0) return 0; return correctChars / totalKeystrokes.
     - No clamping — accuracy is naturally in [0, 1] by construction.

2. Add JSDoc above both functions explicitly citing D-05/D-06 with the verbatim formula text in the comment, plus a one-line note on the ROADMAP-vs-D-05 discrepancy (Pitfall 2 in RESEARCH.md): "ROADMAP §Phase 3 criterion 2 says '30 chars in 30s → 2 WPM'; D-05 formula gives 12 WPM for the same input. Tests verify 12 WPM (D-05 is authoritative). If ROADMAP is the source of truth, fix D-05; if D-05 is the source of truth, fix ROADMAP. Until user confirms, this code follows D-05."

3. Create apps/server/src/__tests__/scoring.test.ts with 9+ tests covering D-05/D-06 spec + edge cases + the spec fixture:
   - Test 1 (E2 spec fixture, 30/30/0 = 12 WPM per D-05): computeNetWpm({ correctChars: 30, uncorrectedErrors: 0, elapsedMs: 30_000 }) === 12. Add comment: "D-05 formula yields 12 WPM; ROADMAP says 2 WPM — discrepancy documented in scoring.ts JSDoc."
   - Test 2 (zero correct → 0 WPM): computeNetWpm({ correctChars: 0, uncorrectedErrors: 0, elapsedMs: 60_000 }) === 0.
   - Test 3 (clamped at zero — D-05 max(0, …)): computeNetWpm({ correctChars: 5, uncorrectedErrors: 10, elapsedMs: 5_000 }) === 0 (heavy errors clamp to 0, not negative).
   - Test 4 (elapsedMs = 0 → 0, not NaN): computeNetWpm({ correctChars: 10, uncorrectedErrors: 0, elapsedMs: 0 }) === 0. (Defensive — game shouldn't hit this, but prevents NaN if clock skew ever causes startsAtServerMs === now.)
   - Test 5 (elapsedMs < 0 → 0): same with elapsedMs: -100. Defensive against clock skew.
   - Test 6 (realistic mid-race): computeNetWpm({ correctChars: 100, uncorrectedErrors: 5, elapsedMs: 30_000 }) === Math.max(0, (20 - 1) / 0.5) === 38 (rounded from 38.0).
   - Test 7 (D-06 accuracy basic): computeAccuracy({ correctChars: 95, totalKeystrokes: 100 }) === 0.95.
   - Test 8 (D-06 zero total → 0): computeAccuracy({ correctChars: 0, totalKeystrokes: 0 }) === 0 (avoids divide-by-zero).
   - Test 9 (D-06 all wrong): computeAccuracy({ correctChars: 0, totalKeystrokes: 50 }) === 0.
   - Test 10 (D-06 perfect): computeAccuracy({ correctChars: 50, totalKeystrokes: 50 }) === 1.

Verify: bun test apps/server/src/__tests__/scoring.test.ts exits 0 (10/10 green); bun run --filter '@typing-race/server' typecheck exits 0.
  </action>
  <verify>
  <automated>bun test apps/server/src/__tests__/scoring.test.ts && bun run --filter '@typing-race/server' typecheck</automated>
  </verify>
  <done>
computeNetWpm + computeAccuracy are pure; spec fixture 30/30/0 = 12 WPM verified (with ROADMAP discrepancy documented); 10 tests green; typecheck clean. Tracer commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 2: validateKeystroke wires computeNetWpm + integration test for live WPM</name>
  <files>apps/server/src/race/validate-keystroke.ts, apps/server/src/__tests__/validate-keystroke.test.ts</files>
  <action>
Per D-05 (broadcast WPM live via cursor_update — Plan 02 ships the field; Plan 03 fills in the real value) + Pattern 3 in RESEARCH.md (validator owns the side-effect):

1. Extend apps/server/src/race/validate-keystroke.ts:
   - Add import: { computeNetWpm } from "./scoring.ts";
   - In the accept path (after charStates are computed but before stamping player), compute elapsedMs = room.startsAtServerMs === null ? 0 : now - room.startsAtServerMs. Defensive against null (Plan 02's "now < startsAtServerMs + grace" check should have already rejected butts is reachable in theory if grace === 0).
   - Replace Plan 02's player.currentWpm = 0 placeholder with: player.currentWpm = computeNetWpm({ correctChars, uncorrectedErrors, elapsedMs }).
   - Update playerPatch.currentWpm to match player.currentWpm in the return.
   - No other changes — Plan 02's structure (newCharStates + playerPatch shape) is correct.

2. Add 3 new tests to apps/server/src/__tests__/validate-keystroke.test.ts (extends 14 to 17):
   - Test 15 (D-05 live WPM updates): set up room.startsAtServerMs = 0; pass now=30000 (30s elapsed); type one correct char at index 0; assert player.currentWpm === computeNetWpm({ correctChars: 1, uncorrectedErrors: 0, elapsedMs: 30000 }) === Math.max(0, (1/5) / 0.5) === 0.4. (Document: with only 1 char, the WPM is tiny — realistic only after ~5 chars / 5s.)
   - Test 16 (D-05 spec fixture via validateKeystroke): set up room.startsAtServerMs = 0; accept 30 correct chars in 30 seconds; assert player.currentWpm === 12 (the D-05 spec fixture — also asserted at the scoring layer in scoring.test.ts).
   - Test 17 (D-05 playerPatch.currentWpm === player.currentWpm): type one char; assert the returned playerPatch.currentWpm === player.currentWpm (consistency between the wire snapshot and server state).

Verify: bun test apps/server/src/__tests__/validate-keystroke.test.ts exits 0 (17/17 green).
  </action>
  <verify>
  <automated>bun test apps/server/src/__tests__/validate-keystroke.test.ts</automated>
  </verify>
  <done>
validateKeystroke stamps real computeNetWpm value into player.currentWpm on every accept; playerPatch.currentWpm matches player.currentWpm. 17 tests green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wave boundary — full typecheck + test + build, surface fixture discrepancy</name>
  <files>(none — verification-only task)</files>
  <action>
Wave 1 close-out for the scoring concern. This task is a verification checkpoint, not new code. Per planner execution discipline (type-check across all workspaces must pass at each wave boundary):

1. Run bun run --filter '*' typecheck — must exit 0. Catches any drift in cursor_update.charStates/wpm usage, scoring.ts exports, or Player field typing.

2. Run bun run --filter '*' test — must exit 0. Aggregate across:
   - packages/shared: passages.test.ts (8), messages.test.ts (8 existing + 3 from Plan 01 + 3 from Plan 02 = 14)
   - apps/server: rooms.test.ts (8), race-controller.test.ts (6 existing — still green after Player fake fixture updates), validate-keystroke.test.ts (8 + 6 from Plan 02 + 3 from this plan = 17), clock.test.ts (2), corpus.test.ts (10), char-states.test.ts (7), scoring.test.ts (10)
   - apps/web: clock.test.ts (4)
   - Total: 74 tests (up from Phase 2's 40 — Plan 01 +02 +03 add 34)

3. Run bun --filter '@typing-race/web' build — must exit 0. Confirms the shared schema extensions (cursor_update gains optional charStates + wpm) don't break the web build.

4. Surface the ROADMAP-vs-D-05 WPM unit discrepancy to the user via the plan output (## PLANNING COMPLETE in the parent task response). Note that the discrepancy is documented in scoring.ts JSDoc and scoring.test.ts comments; no action needed in code; user decides later whether to update ROADMAP.md or D-05.

5. Sanity-check: the tracer comment in scoring.ts JSDoc must cite both ROADMAP criterion 2 AND D-05 with the exact wording from each. This is the audit trail for future readers.

Verify: All 4 commands exit 0; the discrepancy is documented in source.
  </action>
  <verify>
  <automated>bun run --filter '*' typecheck && bun run --filter '*' test && bun --filter '@typing-race/web' build && grep -A 4 "ROADMAP" apps/server/src/race/scoring.ts</automated>
  </verify>
  <done>
Full Wave 1 typecheck + test + build green. ROADMAP-vs-D-05 WPM discrepancy is documented in source. Plan 03 closes; Plan 04 (race-end + results + rematch) unblocks.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validateKeystroke (server-internal) → computeNetWpm → Player.currentWpm | Server-internal; no untrusted input |
| Player.currentWpm → cursor_update broadcast | Server-authoritative; clients render-only |
| Player.charStates / totalKeystrokes → computeAccuracy → race_end.results[].accuracy | Server-authoritative; results aggregation only at race end |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-09 | Tampering | client spoofs elapsedMs via clientTs | low | mitigate | D-05 formula uses server's Date.now() - room.startsAtServerMs, NOT any client-supplied timestamp. Plan 02's validateKeystroke never reads frame.clientTs for timing (anti-cheat #1 implicit). D-05 wired here follows the same pattern: now comes from server's argument, never from frame. |
| T-03-10 | Information Disclosure | WPM broadcast reveals typing speed | low | accept | WPM is public — both players see each other's WPM in cursor_update per D-05. No additional disclosure beyond what D-05 already locks. |
| T-03-11 | DoS | NaN/Infinity from elapsedMs === 0 | low | mitigate | computeNetWpm guards minutes <= 0 → return 0; computeAccuracy guards totalKeystrokes === 0 → return 0. No NaN paths. Tests cover (Test 4, Test 5, Test 8). |
| T-03-SC | Tampering | npm/pip/cargo installs | n/a | n/a | No new dependencies in this plan |
</threat_model>

<verification>
- bun test apps/server/src/__tests__/scoring.test.ts — 10 spec fixtures including the 30/30/0 = 12 WPM spec and the ROADMAP-vs-D-05 discrepancy note
- bun test apps/server/src/__tests__/validate-keystroke.test.ts — 17 tests (8 Phase 2 + 6 Plan 02 + 3 Plan 03)
- bun run --filter '*' typecheck — all workspaces compile clean
- bun run --filter '*' test — full suite (74 tests across shared + server + web) green
- bun --filter '@typing-race/web' build — web build green with extended cursor_update schema
- Manual: grep -A 4 "ROADMAP" apps/server/src/race/scoring.ts — discrepancy documented
</verification>

<success_criteria>
1. computeNetWpm matches D-05 formula verbatim: max(0, (correctChars/5) - (uncorrectedErrors/5)) / minutesElapsed (D-05)
2. computeAccuracy matches D-06: correctChars / totalKeystrokes (D-06)
3. 30 correct chars / 30s / 0 errors = 12 WPM per D-05 (discrepancy with ROADMAP 2 WPM documented in JSDoc + test comments)
4. Zero correct → 0 WPM (not negative); negative errors clamp to 0 (max(0, …) verified)
5. elapsedMs <= 0 → 0 WPM (defensive against clock skew, no NaN)
6. accuracy = 0 when totalKeystrokes === 0 (no divide-by-zero)
7. validateKeystroke calls computeNetWpm on every accept; player.currentWpm matches playerPatch.currentWpm
8. All 74 tests green (8 + 14 shared + 40 server + 4 web — Phase 2 baseline + Plan 01 +02 +03 additions); web build green; ROADMAP-vs-D-05 documented
</success_criteria>

<output>
Create .planning/phases/03-race-track/03-03-SUMMARY.md when done
</output>

## Artifacts this phase produces

**Pure functions / helpers**
- apps/server/src/race/scoring.ts — gains computeNetWpm({ correctChars, uncorrectedErrors, elapsedMs }): number and computeAccuracy({ correctChars, totalKeystrokes }): number (both with JSDoc citing D-05/D-06 verbatim + ROADMAP discrepancy note)

**Server-side integration**
- apps/server/src/race/validate-keystroke.ts — Plan 02 placeholder player.currentWpm = 0 replaced with player.currentWpm = computeNetWpm({ correctChars, uncorrectedErrors, elapsedMs: now - room.startsAtServerMs ?? 0 }); playerPatch.currentWpm matches

**Test artifacts (Wave 0 / Plan 03)**
- apps/server/src/__tests__/scoring.test.ts (NEW) — 10 tests: spec fixture (30/30/0 = 12 WPM), zero correct, clamped at zero, elapsedMs <= 0, mid-race realistic, accuracy basic + zero-total + all-wrong + perfect
- apps/server/src/__tests__/validate-keystroke.test.ts — extended from 14 to 17 tests (live WPM update on single accept, D-05 spec fixture end-to-end, playerPatch consistency)