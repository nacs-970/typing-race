---
phase: 05.1-add-reconnect-progress-bar-fix-version-mismatch-toast-wiring
verified: 2026-09-23T00:24:00Z
status: passed
score: 6/6 must-haves verified (5 code-level truths + ROADMAP.md reconciliation)
covered_files:
  - ".planning/ROADMAP.md"
  - ".planning/phases/05-frontend-polish/05-04-SUMMARY.md"
  - ".planning/phases/05-frontend-polish/05-VERIFICATION.md"
  - ".planning/phases/05.1-add-reconnect-progress-bar-fix-version-mismatch-toast-wiring/05.1-01-SUMMARY.md"
  - ".planning/phases/05.1-add-reconnect-progress-bar-fix-version-mismatch-toast-wiring/05.1-CONTEXT.md"
  - "apps/web/src/App.tsx"
  - "apps/web/src/__tests__/App.test.tsx"
  - "apps/web/src/__tests__/ReconnectBanner.test.tsx"
  - "apps/web/src/__tests__/ToastQueue.test.tsx"
  - "apps/web/src/components/ReconnectBanner.tsx"
  - "apps/web/src/net/race-client.ts"
  - "apps/web/src/store/connection.ts"
covered_digest: "v1:sha256:00a2f318b2b7741410379395141e305d003d6f9dca7393d34b88cbcbfc6bad4e"
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 5/5 (code) + 1 unresolved human item
  gaps_closed:
    - "ROADMAP.md's Phase 05.1 entry (placeholder Goal/Requirements/Plans/unchecked plan line) reconciled with real content in commit ad78c2a"
  gaps_remaining: []
  regressions: []
audit_acknowledged:
  milestone: v1.0
  at: 2026-09-22
  status: passed
---

# Phase 05.1: Add reconnect progress bar, fix version-mismatch toast wiring Verification Report

**Phase Goal:** Close two gaps identified by 05-VERIFICATION.md during v1.0 milestone close: (1) no local-client "Reconnecting… (Ns)" progress bar existed at all, driven by real connection-status state; (2) a version-mismatch toast was claimed in 05-04-SUMMARY.md but never actually wired (no `VERSION_MISMATCH` error code anywhere; the prior test only asserted a hardcoded string via a direct `addToast()` call).

**Verified:** 2026-09-23
**Status:** passed
**Re-verification:** Yes — confirms the sole remaining human_needed item from the 2026-09-16 pass (ROADMAP.md reconciliation) is now genuinely resolved, and re-confirms all 5 code-level truths still hold against the current codebase.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `ReconnectBanner.tsx` exists and is mounted in a reachable position in the render tree | ✓ VERIFIED | `apps/web/src/components/ReconnectBanner.tsx` exists (75 lines, substantive JSX + hooks). Imported at `App.tsx:12`, rendered unconditionally at `App.tsx:367` (`<ReconnectBanner sessionTakenOver={sessionTakenOver} />`) directly inside the top-level `<main>` returned by `App()` — confirmed by direct read of `App.tsx:344-370` in the current working tree, not by trusting SUMMARY. |
| 2 | Banner is driven by real `useConnectionStore().status`, shows only after a connection has been open once and then drops (not on first connect), suppressed during session takeover | ✓ VERIFIED | `ReconnectBanner.tsx:11-21`: reads `useConnectionStore((s) => s.status)`; `hasEverBeenOpenRef` only flips true on `status === "open"`; `isReconnecting = hasEverBeenOpenRef.current && status !== "open" && !sessionTakenOver`. Store→render leg exercised by the 6 tests in truth 3. Upstream WS-event→store leg unchanged since the prior pass (no commits touch `race-client.ts` or `connection.ts` since 2026-09-16) — re-confirmed present by direct file read. |
| 3 | A real test file exists and exercises the banner's behavior | ✓ VERIFIED | `apps/web/src/__tests__/ReconnectBanner.test.tsx` — 6 tests: not-shown-before-first-open, not-shown-while-open, shown-after-drop, elapsed-seconds-counting (`vi.advanceTimersByTime`), hidden-on-reopen, suppressed-by-`sessionTakenOver`. All drive the real Zustand store via `useConnectionStore.setState()`, not a mock. Re-read in full this pass; unchanged since prior verification. |
| 4 | Version-mismatch toast case removed from `ToastQueue.test.tsx`; no dangling `VERSION_MISMATCH` references left anywhere | ✓ VERIFIED | Re-read `ToastQueue.test.tsx` in full: no version-mismatch case present, only Room Lost / Server Restart / Rate Limit cases. `grep -rn -i "version_mismatch\|version mismatch" apps packages` → 0 hits in source/tests. `App.tsx`'s error-code switch has no `VERSION_MISMATCH` branch. |
| 5 | No regression in existing engine/gateway/web test suites | ✓ VERIFIED (see caveat) | Re-ran independently. `bun run typecheck` (all 4 workspaces) → 0 errors. `bun test packages/shared apps/gateway apps/engine` → **153 pass, 0 fail** (121426 expect() calls, 23 files) — one fewer than the prior pass's 154 due to intervening unrelated work (`a63a9ac`, consolidated passage-length-bucketing tests; confirmed via `git show --stat`, not a 05.1-scope regression). `bun run --cwd apps/web test -- --run`: **first run** 88 passed / 1 failed (`App.test.tsx > renders a distinct 'Server Restarting' toast…`, 5000ms timeout); **isolated single-test run** (`-t "renders a distinct"`) passed; **second full run** 89 passed / 0 failed. The failure did not reproduce on either re-run — genuinely flaky under full-suite parallel load, not a deterministic break. `App.test.tsx` was added by commit `f1c5af0` for **Phase 06-04**, postdating 05.1 by several commits; none of 05.1's own files (`ReconnectBanner.tsx`, `ReconnectBanner.test.tsx`, `ToastQueue.test.tsx`, `App.tsx`'s `ReconnectBanner` mount) have been touched since 05.1's own commit `cdd3a86`. This is out of 05.1's scope and not attributable to this phase. |
| 6 | ROADMAP.md's Phase 05.1 entry reflects the phase's actual completed state | ✓ VERIFIED | Read `.planning/ROADMAP.md` lines 170-180 directly. `**Goal:**` now reads real text ("Add the local-client 'Reconnecting… (5s)' progress bar that 05-04 claimed but never wired…"), `**Requirements**: REQ-06` (was `TBD`), `**Plans:** 1/1 plans complete` (was `0 plans`), plan line reads `- [x] 05.1-01: ReconnectBanner component added and mounted in App.tsx…` (was unchecked `- [ ] TBD`). Matches commit `ad78c2a` ("docs(roadmap): fill in placeholder Goal/Requirements/Plans for 03.1/04.1/05.1/06.1"), confirmed via `git show ad78c2a --stat`. This closes the sole human_needed item from the 2026-09-16 pass. |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `apps/web/src/components/ReconnectBanner.tsx` | Reconnect progress bar component | ✓ VERIFIED | Substantive, wired into `App.tsx`, data flows from real `useConnectionStore` |
| `apps/web/src/__tests__/ReconnectBanner.test.tsx` | Behavioral test coverage | ✓ VERIFIED | 6 tests exercising real state transitions against the real Zustand store |
| `apps/web/src/App.tsx` | Mounts `ReconnectBanner` reachably | ✓ VERIFIED | Imported line 12, rendered line 367 in top-level unconditional render tree |
| `apps/web/src/__tests__/ToastQueue.test.tsx` | Version-mismatch fixture-only assertion removed | ✓ VERIFIED | No `VERSION_MISMATCH`/"Version Mismatch" reference remains |
| `.planning/ROADMAP.md` | Phase 05.1 entry reconciled with real content | ✓ VERIFIED | Real Goal, REQ-06, 1/1 plans complete, checked-off plan line (commit `ad78c2a`) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `App.tsx` | `ReconnectBanner` | JSX mount, `sessionTakenOver` prop | ✓ WIRED | Confirmed by direct read of `App.tsx:344-370` |
| `ReconnectBanner` | `useConnectionStore` | `useConnectionStore((s) => s.status)` hook | ✓ WIRED | Real store subscription, not a prop-drilled mock |
| `ROADMAP.md` Phase 05.1 entry | `05.1-01-SUMMARY.md` | Documentation reconciliation, commit `ad78c2a` | ✓ WIRED | Requirements/Plans/Goal text now match the completed SUMMARY |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| `ReconnectBanner` | `status` | `useConnectionStore` ← `race-client.ts` WS `open`/`close` event listeners | Yes — real WebSocket lifecycle events | ✓ FLOWING |
| `ReconnectBanner` | `elapsedMs` / `seconds` / `percent` | `Date.now()` delta since drop, ticked by a real `setInterval` | Yes | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Typecheck (all 4 workspaces) | `bun run typecheck` | 0 errors | ✓ PASS |
| Backend test suite (no regression) | `bun test packages/shared apps/gateway apps/engine` | 153 pass, 0 fail (was 154 prior pass; explained by unrelated intervening commit `a63a9ac`) | ✓ PASS |
| Web test suite, first full run | `bun run --cwd apps/web test -- --run` | 88 passed, 1 failed (`App.test.tsx`, 5000ms timeout), exit 1 | ✗ FAIL (transient) |
| Web test suite, isolated failing test | `bun run --cwd apps/web test -- --run -t "renders a distinct"` | 1 passed | ✓ PASS |
| Web test suite, second full run | `bun run --cwd apps/web test -- --run` | 89 passed, 0 failed | ✓ PASS |
| No dangling VERSION_MISMATCH reference | `grep -rn -i "version_mismatch\|version mismatch" apps packages` | 0 hits outside `.planning/` docs | ✓ PASS |
| ReconnectBanner reachable in App render tree | Direct read of `App.tsx:344-370` | Unconditional render inside `<main>` | ✓ PASS |
| ROADMAP.md Phase 05.1 reconciliation | Direct read of `.planning/ROADMAP.md:170-180` | Real Goal/REQ-06/1 plan/checked box | ✓ PASS |

### Decision Coverage

Skipped cleanly — `05.1-CONTEXT.md` has no `<decisions>` block (it documents diagnosed gaps and required fixes, not a `<decisions>` list).

### Requirements Coverage

| Requirement | Source | Description | Status | Evidence |
|-------------|--------|-------------|--------|----------|
| REQ-06 | 05.1-01-SUMMARY.md frontmatter (`requirements-completed`) | Reconnect UX / connection-state correctness | ✓ SATISFIED | `ReconnectBanner` closes roadmap SC4 clause 1; toast copywriting-contract corrected. Now also reflected in `.planning/ROADMAP.md`'s Phase 05.1 entry. |

Note: `.planning/REQUIREMENTS.md` does not exist in this project; requirement tracking is via ROADMAP.md phase mapping and SUMMARY frontmatter.

### Anti-Patterns Found

No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers in `ReconnectBanner.tsx`, `App.tsx`'s modified region, `ReconnectBanner.test.tsx`, or `ToastQueue.test.tsx`. No stub components, no empty handlers, no hardcoded-empty props flowing to render.

**Info (non-blocking):** `App.test.tsx` (Phase 06-04's SERVER_SHUTTING_DOWN regression test, not a 05.1 artifact) timed out once under full-suite parallel load and passed on two subsequent re-runs (isolated and full). Root cause not fully diagnosed here (test's own comment flags a WS singleton imported at module load across 13 test files as a plausible source of cross-file contention under load) but confirmed non-deterministic, unrelated to 05.1's own code, and not a regression this phase introduced — none of 05.1's files have changed since its own commit `cdd3a86`. Flagged for awareness, not a gap.

### Human Verification Required

None. The one item from the 2026-09-16 pass (ROADMAP.md Phase 05.1 reconciliation) is now confirmed resolved by direct inspection — see truth 6 above.

### Gaps Summary

None. All 5 code-level must-haves from the original gap-closure phase remain verified against the current codebase, and the single outstanding documentation item (ROADMAP.md's Phase 05.1 entry) is confirmed reconciled in commit `ad78c2a`. Phase goal fully achieved.

---

_Verified: 2026-09-23T00:24:00Z_
_Verifier: Claude (gsd-verifier)_
