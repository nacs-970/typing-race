---
phase: 03.1-wire-word-correctness-into-output-frame
verified: 2026-09-23T00:00:00Z
status: closed_via_override
score: n/a (override, not a build) — 4/4 re-confirmation truths verified
covered_files:
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/phases/03-race-track/03-CONTEXT.md
  - .planning/phases/03-race-track/03-VERIFICATION.md
  - .planning/phases/03.1-wire-word-correctness-into-output-frame/03.1-01-SUMMARY.md
  - .planning/phases/03.1-wire-word-correctness-into-output-frame/03.1-CONTEXT.md
covered_digest: "v1:sha256:a319877669f2469f12b445c402bdf9853c52780512aa4dbf189675ea733b0e6a"
behavior_unverified: 0
overrides_applied: 1
re_verification:
  previous_status: closed_via_override
  previous_score: "n/a (override, not a build)"
  gaps_closed: []
  gaps_remaining: []
  regressions: []
gaps: []
deferred: []
advisory: []
human_verification: []
status_vocabulary_note: >
  `closed_via_override` is not in this verifier's baseline passed|gaps_found|human_needed
  vocabulary. Retained deliberately, matching this project's own convention (used
  identically by the sibling 03-VERIFICATION.md and by the previous 2026-09-17 pass of
  this same file) — not a schema error.
---

# Phase 03.1: Wire word-correctness into output frame Verification Report

**Phase Goal (original):** Wire `aggregateWordCorrectness`/`isWordCorrect` (apps/engine/src/race/scoring.ts)
into the real race-state/wire-protocol output so per-word correctness reaches the client and is
rendered — closing the gap 03-VERIFICATION.md's Gap B identified ("No player ever sees per-word
correctness").

**Resolved outcome (unchanged by this re-verification):** Not a real gap. `03-CONTEXT.md` decision
D-13 explicitly scopes `aggregateWordCorrectness`/`isWordCorrect` to the server data model, "not UI."
An initial build attempt wired per-word correctness into the wire protocol and `RaceView.tsx` before
D-13 was found; that attempt was reverted. This re-verification's job is to confirm the revert is
still intact in the current codebase and that nothing since 2026-09-17 quietly reintroduced it.

**Verified:** 2026-09-23
**Status:** closed via override — confirmed still correct, no rebuild needed
**Re-verification:** Yes — re-confirms the 2026-09-17 `closed_via_override` pass; this phase was
flagged "stale" by init.manager solely because files it covers (`03-VERIFICATION.md`) were edited
after 2026-09-17 to record human sign-off and sync status fields, not because the reverted behavior
changed. See "Why this phase went stale" below.

## Goal Achievement

### Observable Truths (re-confirmation)

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | The reverted UI-rendering attempt (wire-protocol `words` field on `cursor_update`/`rejoined_room`, `word-correct`/`word-incorrect` CSS classes in `RaceView.tsx`, `apps/web/src/core/word-correctness.ts` helper) has not been reintroduced | ✓ VERIFIED | `grep -n "words"` on `cursorUpdateSchema`/`rejoinedRoomSchema` in `packages/shared/src/messages.ts` — no `words` field (only an unrelated `random_words` corpus-type literal). `grep -i "word"` on `apps/web/src/components/RaceView.tsx` — 0 matches. `grep -i "word"` on `apps/web/src/styles.css` — only unrelated CSS properties (`word-break`, `word-spacing`). `apps/web/src/core/word-correctness.ts` does not exist. Full diff of every commit touching `messages.ts`, `apps/engine/src/race/frames.ts`, `apps/web/src/components/RaceView.tsx`, `apps/web/src/store/race.ts`, `apps/web/src/net/race-client.ts`, `apps/engine/src/engine.ts` since 2026-09-17 shows zero `+`-side lines containing "word" (all "word" hits are `-`-side, i.e. the original f74b721 revert itself) |
| 2 | `aggregateWordCorrectness`/`isWordCorrect` remain unused-but-intentional (D-13) — not called from any production code path | ✓ VERIFIED | Repo-wide `grep -rn "aggregateWordCorrectness\|isWordCorrect" apps/ packages/` returns exactly 2 production definitions (`apps/engine/src/race/scoring.ts:34,58`) plus references only from their own test file (`apps/engine/src/__tests__/char-states.test.ts`, whose `describe` block is literally titled "aggregateWordCorrectness — D-13 + Pitfall 7"). Zero references from `engine.ts`, `frames.ts`, or any `apps/web` file |
| 3 | No regression in existing engine/gateway/web/shared test suites attributable to this phase's revert | ✓ VERIFIED (with one unrelated, out-of-scope observation) | `bun test packages/shared apps/gateway apps/engine`: 153/153 pass. `apps/web` (`vitest run`): 88/89 pass — the 1 failure (`App — SERVER_SHUTTING_DOWN error frame > renders a distinct 'Server Restarting' toast...`, timeout) is unrelated to word-correctness: it belongs to phase `06-04` (`git log -S"SERVER_SHUTTING_DOWN" -- apps/web/src/__tests__/App.test.tsx` → commit `f1c5af0 test(06-04): add App.tsx SERVER_SHUTTING_DOWN toast regression test`), is not in this phase's file set, was not touched by any commit since the 03.1 revert, and passes 1/1 when run in isolation (`npx vitest run src/__tests__/App.test.tsx` and `-t "<name>"` both green) — it fails reproducibly only under full-suite parallel execution, indicating cross-test/module-load contention scoped to phase 06-04's own test, not a defect this phase introduced or should fix |
| 4 | The original report's "Follow-up needed" (human sign-off on Gap A/B overrides, then `accepted_by: PENDING`) is resolved | ✓ VERIFIED | `03-VERIFICATION.md` now shows both Gap A and Gap B overrides with `accepted_by: "atithep_thepkit@cmu.ac.th"`, `accepted_at: "2026-09-21"`, plus `resolved: 2026-09-21T00:00:00Z` and `audit_acknowledged: {milestone: v1.0, at: 2026-09-22, status: closed_via_override}` in its frontmatter. The PENDING language from the 2026-09-17 pass of this file is stale and is not carried forward |

**Score:** 4/4 re-confirmation truths verified (0 present-but-behavior-unverified)

### Why this phase went stale (and why that's not a regression)

`git log --since=2026-09-17 -- <covered_files>` shows exactly 3 commits touching this phase's
covered files, all after the 2026-09-17 verification timestamp:

- `eb26a29` (2026-09-21) — `docs(v1.0): record human sign-off on Gap A/B overrides in 03-VERIFICATION.md`
- `d6042c4` (2026-09-23) — `fix(docker): shrink runtime image...` (touches `03-VERIFICATION.md` only
  incidentally, to sync a stale status field: "03's Gap A/B already had human sign-off recorded
  in-file... but the status field was never updated to match")
- `0d1028a` (2026-09-23) — `chore(v1.0): resolve pre-close verification audit gaps` (adds
  `audit_acknowledged` to `03-VERIFICATION.md` alongside five other phases' VERIFICATION.md files)

All three are documentation/status-sync commits directly continuing this phase's own gap-closure
paper trail — not, as initially guessed, unrelated Dockerfile or `styles.css` theme-work noise
(neither of those files is in this phase's `covered_files`, and confirmed above they carry no
word-correctness content regardless). None of the three touched `RaceView.tsx`, the wire protocol,
or any reverted behavior. The staleness flag is a true positive on "a covered file changed" and a
false positive on "the reverted behavior might have come back" — this re-verification confirms the
latter did not happen.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `packages/shared/src/messages.ts` | No `words` field on `cursorUpdateSchema`/`rejoinedRoomSchema` | ✓ VERIFIED | Confirmed absent; schemas match pre-03.1 baseline shape |
| `apps/web/src/components/RaceView.tsx` | No word-correct/word-incorrect rendering, per-char accents only | ✓ VERIFIED | 0 "word" matches; renders `char-${state}` per Phase 3's original (2-tone, Gap A) model only |
| `apps/web/src/core/word-correctness.ts` | Should not exist (deleted during revert) | ✓ VERIFIED | File absent |
| `apps/engine/src/race/scoring.ts` | `aggregateWordCorrectness`/`isWordCorrect` still defined, still unconsumed | ✓ VERIFIED | Present, unchanged in intent, only consumed by `char-states.test.ts` |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `apps/engine/src/race/scoring.ts` `aggregateWordCorrectness` | *(none)* | *(none found)* | ✗ NOT WIRED (intentional, per D-13 + accepted override) | Matches 03-VERIFICATION.md Gap B's accepted-override resting state; this is the expected, correct state, not a defect |
| `RaceView.tsx` | wire protocol `cursor_update`/`rejoined_room` | per-char `charStates` only | ✓ WIRED | Confirms only the pre-03.1 baseline char-level data flows; no word-level field exists to wire |

### Behavioral Spot-Checks / Test Runs

| Behavior | Command | Result | Status |
|---|---|---|---|
| Backend suite (shared+gateway+engine) regression | `bun test packages/shared apps/gateway apps/engine` | 153 pass / 0 fail | ✓ PASS |
| `aggregateWordCorrectness`/`isWordCorrect` unit tests still valid | (included in engine run above, `char-states.test.ts`) | pass | ✓ PASS |
| Web suite regression | `npx vitest run` (apps/web) | 88 pass / 1 fail | ⚠️ 1 unrelated failure — see Truth #3; isolated re-run of the failing test/file is green |
| Isolated re-run of the one failing web test | `npx vitest run src/__tests__/App.test.tsx` | 1 pass / 0 fail | ✓ PASS (confirms full-suite-only flake, not a real regression) |

### Requirements Coverage

None — ROADMAP.md records `Requirements: none` for Phase 03.1 ("see override below"); no
`.planning/REQUIREMENTS.md` exists in this project. `03.1-01-SUMMARY.md`'s original
`requirements-completed: [REQ-05, REQ-08]` claim was already retracted in the prior verification pass
(REQ-05/REQ-08 were satisfied by Phase 3 itself, not this phase) — that retraction still holds; no new
requirement claims were introduced since.

### Anti-Patterns Found

None in the covered files. No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers found in
`03.1-CONTEXT.md`, `03.1-01-SUMMARY.md`, or the relevant sections of `03-VERIFICATION.md`.

### Minor Doc Drift (non-blocking)

`03.1-01-SUMMARY.md`'s prose says the 6 word-correctness unit tests live in `scoring.test.ts`; they
actually live in `apps/engine/src/__tests__/char-states.test.ts` (confirmed by file content, whose
`describe` block cites D-13 by name). This is stale documentation, not a code or verification defect,
and is called out here rather than silently corrected.

### Human Verification Required

None.

### Gaps Summary

No gaps. This re-verification confirms the 2026-09-17 `closed_via_override` resolution still holds:
the word-correctness UI-rendering revert is fully intact (wire protocol, `RaceView.tsx`, and the
deleted helper module all match the pre-03.1 baseline), `aggregateWordCorrectness`/`isWordCorrect`
remain correctly unused per D-13, both Gap A and Gap B overrides in `03-VERIFICATION.md` now carry
an explicit human `accepted_by`/`accepted_at` (resolving this phase's prior "Follow-up needed" item),
and the "stale" flag was caused by legitimate status-sync commits to `03-VERIFICATION.md` rather than
by any reintroduction of the reverted behavior. One pre-existing, out-of-scope test
(`App.test.tsx`'s `SERVER_SHUTTING_DOWN` toast test, owned by phase 06-04) fails only under
full-suite parallel execution and passes in isolation — flagged for phase 06-04's owner, not a 03.1
concern.

---
_Verified: 2026-09-23_
_Verifier: Claude (gsd-verifier), re-verification pass_
