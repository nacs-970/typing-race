---
phase: 05.1-add-reconnect-progress-bar-fix-version-mismatch-toast-wiring
verified: 2026-09-16T23:25:00Z
status: human_needed
score: 5/5 must-haves verified
covered_files:
  - ".planning/phases/05-frontend-polish/05-04-SUMMARY.md"
  - ".planning/phases/05-frontend-polish/05-VERIFICATION.md"
  - ".planning/phases/05.1-add-reconnect-progress-bar-fix-version-mismatch-toast-wiring/05.1-01-SUMMARY.md"
  - ".planning/phases/05.1-add-reconnect-progress-bar-fix-version-mismatch-toast-wiring/05.1-CONTEXT.md"
  - "apps/web/src/App.tsx"
  - "apps/web/src/__tests__/ReconnectBanner.test.tsx"
  - "apps/web/src/__tests__/ToastQueue.test.tsx"
  - "apps/web/src/components/ReconnectBanner.tsx"
  - "apps/web/src/net/race-client.ts"
  - "apps/web/src/store/connection.ts"
covered_digest: "v1:sha256:cce1dd498c16dd815bbef964d87a5076b989ad224a677de04e43da80a77a537b"
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 2/5
  gaps_closed:
    - "Disconnect shows 'Reconnecting… (5s)' progress bar (Success Criterion 4, clause 1)"
    - "Distinct error toast for 'version mismatch' case — fixture-only unreachable test removed"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Reconcile .planning/ROADMAP.md's Phase 05.1 entry against the completed plan: update `**Goal:**` (currently the placeholder `[Urgent work - to be planned]`), `**Requirements**:` (currently `TBD` — should reflect REQ-06 per 05.1-01-SUMMARY.md's `requirements-completed`), `**Plans:**` (currently `0 plans`), and check the `- [ ] TBD (run /gsd-plan-phase 05.1 to break down)` box to `- [x] 05.1-01: ...` now that 05.1-01-SUMMARY.md exists with `status: complete`."
    expected: "ROADMAP.md's Phase 05.1 section matches the actual completed state: real goal text, REQ-06 listed, 1 plan, checked-off plan line."
    why_human: "This is a documentation-consistency decision (what goal text to write, whether to keep the phase as a lightweight gap-closure insert vs. retroactively formalize it with /gsd-plan-phase) rather than a code-correctness fact a grep/test can settle. The code-side fix (ReconnectBanner + toast wiring) is fully verified and does not depend on this; only the roadmap bookkeeping is unresolved."
---

# Phase 05.1: Add reconnect progress bar, fix version-mismatch toast wiring Verification Report

**Phase Goal:** Close two gaps identified by 05-VERIFICATION.md during v1.0 milestone close: (1) no local-client "Reconnecting… (Ns)" progress bar existed at all, driven by real connection-status state; (2) a version-mismatch toast was claimed in 05-04-SUMMARY.md but never actually wired (no `VERSION_MISMATCH` error code anywhere; the prior test only asserted a hardcoded string via a direct `addToast()` call).

**Verified:** 2026-09-16
**Status:** human_needed
**Re-verification:** Yes — this is the gap-closure phase for the 2 FAILED truths in `.planning/phases/05-frontend-polish/05-VERIFICATION.md`

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `ReconnectBanner.tsx` exists and is mounted in a reachable position in the render tree | ✓ VERIFIED | `apps/web/src/components/ReconnectBanner.tsx` exists (75 lines, substantive JSX + hooks, no stub). Imported at `App.tsx:12` and rendered unconditionally at `App.tsx:348` (`<ReconnectBanner sessionTakenOver={sessionTakenOver} />`) directly inside the top-level `<main>` tree returned by `App()` — not nested in any dead branch, not gated behind an unreachable condition. Confirmed by reading `App.tsx` lines 326-370 directly, not by trusting the SUMMARY. |
| 2 | Banner is driven by real `useConnectionStore().status`, shows only after a connection has been open once and then drops (not on first connect), suppressed during session takeover | ✓ VERIFIED | `ReconnectBanner.tsx:11-21`: reads `useConnectionStore((s) => s.status)`; `hasEverBeenOpenRef` only flips true when `status === "open"`; `isReconnecting = hasEverBeenOpenRef.current && status !== "open" && !sessionTakenOver` — never shows on first `"connecting"` state before any successful open. The store→render leg (state transitions in → banner show/hide/suppress out) is exercised by the 6 tests in row 3, which drive `useConnectionStore.setState()` directly. The upstream WS-event→store leg — `setConnectionStore({status:"connecting"})` on connect attempt (`race-client.ts:72`), `{status:"open"}` on WS open (line 78), `{status:"closed"}` on WS close (line 98) followed by a real `setTimeout(() => this.connect(), 1000)` reconnect loop (line 100) — is verified by direct code inspection, not by an automated test (it is a one-line, non-branching assignment on each real WS lifecycle event, so inspection is adequate evidence, but it should be described accurately rather than implied as test-covered). Session-takeover suppression traced end-to-end: server `session_taken_over` message sets `race-client.ts`'s internal `this.sessionTakenOver = true` (line 313, which also blocks the auto-reconnect loop) AND separately triggers `App.tsx`'s `session_taken_over` handler (line 118-119) which sets React state `sessionTakenOver` — the same prop passed into `ReconnectBanner`. Both paths driven by the same wire message, consistent semantics. |
| 3 | A real test file exists and exercises the banner's behavior | ✓ VERIFIED | `apps/web/src/__tests__/ReconnectBanner.test.tsx` — 6 tests: not-shown-before-first-open, not-shown-while-open, shown-after-drop, elapsed-seconds-counting (via `vi.advanceTimersByTime`), hidden-on-reopen, suppressed-by-`sessionTakenOver`. All drive the component through `useConnectionStore.setState()` (the real store, not a mock) and assert on `data-testid="reconnect-banner"` presence/absence and rendered text — genuine behavioral coverage of the store→render state-transition truth. |
| 4 | Version-mismatch toast case removed from `ToastQueue.test.tsx`; no dangling `VERSION_MISMATCH` references left anywhere that would now be untested/broken | ✓ VERIFIED | Diffed `cdd3a86`'s change to `ToastQueue.test.tsx` directly (`+2/-12`): removes the "Version Mismatch" `addToast()` call and its assertion, and realigns the "Server Restart" body text to `"The server is restarting for maintenance. Please wait a moment and try rejoining."` — matching what `App.tsx:202-205` actually emits for `SERVER_SHUTTING_DOWN`. No unrelated changes in this file's diff. `grep -rn -i "version_mismatch\|version mismatch" apps packages` returns zero hits in any `.ts`/`.tsx` source or test file (only historical references remain in `.planning/` docs — 05-UI-SPEC.md, 05-04-PLAN.md, 05-VERIFICATION.md — expected, as historical planning artifacts, not live code/tests). `App.tsx`'s error-code switch (lines 179-221) has no `VERSION_MISMATCH` branch and never did. |
| 5 | No regression in existing engine/gateway/web test suites | ✓ VERIFIED | Ran independently (not trusting SUMMARY's claimed counts): `bun run typecheck` → clean across `packages/shared`, `apps/web`, `apps/gateway`, `apps/engine` (0 errors). `bun test packages/shared apps/gateway apps/engine` → **154 pass, 0 fail** (121426 expect() calls, 23 files) — matches SUMMARY's claimed 154/154. `bun run --cwd apps/web test -- --run` → **85 passed (85)**, 13 test files — matches SUMMARY's claimed 85/85 (79 prior + 6 new `ReconnectBanner` tests). |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `apps/web/src/components/ReconnectBanner.tsx` | Reconnect progress bar component | ✓ VERIFIED | Exists, substantive (state machine + progress-bar render), wired into `App.tsx`, data flows from real `useConnectionStore` |
| `apps/web/src/__tests__/ReconnectBanner.test.tsx` | Behavioral test coverage | ✓ VERIFIED | 6 tests exercising real state transitions against the real Zustand store |
| `apps/web/src/App.tsx` | Mounts `ReconnectBanner` reachably | ✓ VERIFIED | Imported line 12, rendered line 348 in top-level unconditional render tree |
| `apps/web/src/__tests__/ToastQueue.test.tsx` | Version-mismatch fixture-only assertion removed | ✓ VERIFIED | Diff confirmed to contain only the two documented changes; no `VERSION_MISMATCH`/"Version Mismatch" reference remains |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `App.tsx` | `ReconnectBanner` | JSX mount, `sessionTakenOver` prop | ✓ WIRED | Confirmed by direct read of `App.tsx:326-370`, not SUMMARY trust |
| `ReconnectBanner` | `useConnectionStore` | `useConnectionStore((s) => s.status)` hook | ✓ WIRED | Real store subscription, not a prop-drilled mock |
| `race-client.ts` WS lifecycle | `useConnectionStore` | `setConnectionStore({status: ...})` on connect/open/close | ✓ WIRED (verified by inspection, not by test — see truth 2) | Real `connecting`→`open`→`closed`→(1s timer)→`connecting` state machine drives the store the banner reads |
| `session_taken_over` server message | `ReconnectBanner` suppression | `App.tsx` `sessionTakenOver` state → prop | ✓ WIRED | Same wire event also halts `race-client.ts`'s own reconnect loop — consistent semantics, no fork in behavior |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| `ReconnectBanner` | `status` | `useConnectionStore` ← `race-client.ts` WS `open`/`close` event listeners | Yes — real WebSocket lifecycle events, not a static/mock value | ✓ FLOWING |
| `ReconnectBanner` | `elapsedMs` / `seconds` / `percent` | `Date.now()` delta since drop, ticked by a real `setInterval` | Yes | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Typecheck (all 4 workspaces) | `bun run typecheck` | 0 errors | ✓ PASS |
| Backend test suite (no regression) | `bun test packages/shared apps/gateway apps/engine` | 154 pass, 0 fail | ✓ PASS |
| Web test suite incl. new ReconnectBanner tests | `bun run --cwd apps/web test -- --run` | 85 passed (85), 13 files | ✓ PASS |
| No dangling VERSION_MISMATCH reference in source/tests | `grep -rn -i "version_mismatch\|version mismatch" apps packages` | 0 hits outside `.planning/` docs | ✓ PASS |
| ReconnectBanner reachable in App render tree | Direct read of `App.tsx:326-370` | Unconditional render inside `<main>` | ✓ PASS |
| `ToastQueue.test.tsx` diff contains only documented changes | `git show cdd3a86 -- apps/web/src/__tests__/ToastQueue.test.tsx` | `+2/-12`: server-restart copy realignment + version-mismatch removal only | ✓ PASS |

### Requirements Coverage

| Requirement | Source | Description | Status | Evidence |
|-------------|--------|-------------|--------|----------|
| REQ-06 | 05.1-01-SUMMARY.md frontmatter (`requirements-completed`) | Reconnect UX / connection-state correctness (extends Phase 5's REQ-06 live-cursor/UX scope) | ✓ SATISFIED in code | `ReconnectBanner` closes the roadmap SC4 clause 1 gap; toast copywriting-contract corrected to match actual production wiring. **Not yet reflected in `.planning/ROADMAP.md`'s Phase 05.1 entry** — see Human Verification Required. |

Note: `.planning/REQUIREMENTS.md` does not exist in this project (confirmed by absent-file check); requirement tracking is done via ROADMAP.md phase mapping and SUMMARY frontmatter, consistent with how `05-VERIFICATION.md` handled this.

### Anti-Patterns Found

No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers in any file this phase's commit (`cdd3a86`) modified. No stub `return null`-only components, no empty handlers, no hardcoded-empty props flowing to render in `ReconnectBanner.tsx` or the modified test files.

`.planning/ROADMAP.md` itself does contain unreferenced `TBD` markers in its Phase 05.1 section (`**Requirements**: TBD`, `- [ ] TBD (run /gsd-plan-phase 05.1 to break down)`) — but `git show --stat cdd3a86` confirms ROADMAP.md was **not** a file modified by this phase's commit, so the Step 7 debt-marker gate does not fire on it as a phase-scoped blocker. It is instead surfaced as the human-verification item above, since the phase's own code deliverable is complete but the roadmap record of that fact is stale/placeholder and a reasonable reviewer would expect it reconciled before milestone close.

**Note (informational, not a gap):** commit `cdd3a86`'s diff to `App.tsx` also adds a `ROOM_CLOSED` error-code toast branch (lines 193-195) that is not mentioned anywhere in `05.1-CONTEXT.md` or `05.1-01-SUMMARY.md`. This is legitimate, correctly-wired code (the `ROOM_CLOSED` code is genuinely emitted server-side by `apps/engine/src/rooms/manager.ts:151`, added in the concurrently-worked Phase 04.1 gap-closure — see commit `97a0e8e` "proactively notify remaining clients when a room closes"; server-side coverage confirmed in `apps/engine/src/__tests__/rooms.test.ts:84`). It does not affect this phase's own goal or introduce a defect, and the SUMMARY's own "Issues Encountered" section already discloses that this phase was delegated concurrently with 03.1/04.1/06.1 in a shared working tree. Flagged here only so the documentation-vs-diff mismatch is on record; not a blocker.

### Human Verification Required

See `human_verification` in frontmatter — 1 item: reconcile `.planning/ROADMAP.md`'s Phase 05.1 entry (placeholder `Goal`, `Requirements: TBD`, `Plans: 0`, unchecked `- [ ] TBD` line) against the now-completed `05.1-01-SUMMARY.md`. This is a documentation-consistency decision, not a code-correctness question — all 5 code-level must-haves above are fully verified with no ambiguity.

### Gaps Summary

No code gaps. Both FAILED truths from `05-VERIFICATION.md` are closed:

1. **Reconnect progress bar** — was entirely missing; now exists as `ReconnectBanner.tsx`, correctly mounted, driven by real connection-status state transitions (not a static/mock), with 6 passing behavioral tests exercising the exact state-transition truth (first-connect suppression, drop-after-open trigger, elapsed-time counting, reopen-hides, session-takeover-suppression).
2. **Version-mismatch toast overclaim** — the fixture-only test asserting an unreachable string is removed; no `VERSION_MISMATCH` error code was built (correctly judged unnecessary — this project has no independently-versioned client/server deploy, so there is no real version-skew scenario to guard against) and no dangling reference to it remains anywhere in source or tests.

One human-verification item remains: `.planning/ROADMAP.md`'s Phase 05.1 entry needs to be reconciled to reflect the phase's actual completed status (see above). One additional informational (non-blocking) note is recorded for cleanup awareness: an undocumented but correct `ROOM_CLOSED` addition bundled into this commit, attributable to concurrent Phase 04.1 work.

---

_Verified: 2026-09-16T23:25:00Z_
_Verifier: Claude (gsd-verifier)_
