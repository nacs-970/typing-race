---
context: phase
phase: 06-deploy-hardening
task: 1
total_tasks: 2
status: in_progress
last_updated: 2026-09-07T19:45:27.044Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: agy-delegate whole-phase timeout — a single `agy-job start --tier pro` call given all 4 plan files (06-01..06-04) at once timed out ("timeout waiting for response", rc=2) after burning ~210K tokens (194K input, 1.96M cache_read) with empty stdout. Files were still partially written to disk via mid-run tool calls before the timeout, but nothing was tested or committed. **Structural mitigation: delegate one plan per agy-job/agy-delegate call, verify + commit each before starting the next. Never batch multiple wave-independent plans into a single delegation call for this phase.**

**Do not proceed until this box is checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Whole-phase single delegation | Handing an entire multi-plan GSD phase to one agy-delegate/agy-job call risks a response timeout with no commit and no clear stdout diagnostic (empty out file; real error was in the job's `err` file, not the status command's summary) | blocking | Split delegation per plan; after each, run `agy-job status <id>` AND inspect `~/.antigravity-jobs/<id>/err` directly if state=failed — the status summary alone ("agy exited 1") hid the real cause ("timeout waiting for response") |

_Remove rows that do not apply. The discuss-phase and execute-phase workflows parse this table and enforce a mandatory understanding check for any `blocking` rows._

<current_state>
On branch `phase-06-deploy-hardening` (created off master, which was itself already dirty with unrelated untracked planning files predating this session). Delegated phase 06 (deploy-hardening, 4 plans) to agy via a background `agy-job` (Gemini 3.1 Pro High, `--yolo`, `--dir .`) asking it to execute all 4 plans across 3 dependency waves and commit atomically per plan. The job ran, wrote most of 06-01's code, then timed out before running tests or making any commit. No commits exist on this branch beyond what was on master.

Plan 06-01 (graceful shutdown, wave 1, no deps) is the only one touched:
- Files modified per its `files_modified` list: `packages/shared/src/bridge.ts`, `packages/shared/src/messages.ts`, `apps/engine/src/engine.ts`, `apps/engine/src/index.ts`, `apps/gateway/src/ws/client-manager.ts`, `apps/gateway/src/ws/handlers.ts`, `apps/gateway/src/ws/dispatch.ts`, `apps/gateway/src/index.ts` — all touched (9 files changed, 137 insertions, 4 deletions per `git diff --stat`).
- New test files created: `apps/engine/src/__tests__/drain.test.ts` (131 lines), `apps/gateway/src/__tests__/drain.test.ts` (98 lines) — both untracked, unrun.
- 06-02, 06-04, 06-03 (bun-version pin, App.tsx toast, anti-cheat regression tests) are **untouched** — no `.bun-version` file exists, no Dockerfile changes, no App.tsx changes, no anti-cheat test file.
</current_state>

<completed_work>

Completed Tasks:
- None fully verified/committed. 06-01's code changes exist on disk but are unreviewed, untested, and uncommitted.
</completed_work>

<remaining_work>

- Plan 06-01: review the diff against every `must_haves.truths` and `must_haves.prohibitions` entry in `06-01-PLAN.md` (esp. the 90s timeout must NOT be silently shortened — D-02), run `bun test` for `apps/engine` and `apps/gateway`, fix any gaps, then commit as one atomic commit ("feat(06-01): ...").
- Plan 06-02: `.bun-version` = `1.3.2` at repo root, pin all 4 Dockerfiles to `oven/bun:1.3.2` (matching existing base tag style), add `packages/shared/src/__tests__/bun-version-pin.test.ts`. Wave 1, no deps — can run in parallel with 06-01 but was never started.
- Plan 06-04: `apps/web/src/App.tsx` renders the `SERVER_SHUTTING_DOWN` frame (from 06-01's `ClientManager.broadcastAll`) as a distinct friendly toast, not the generic fallback. Depends on 06-01.
- Plan 06-03: anti-cheat regression tests for `apps/engine/src/race/validate-keystroke.ts` (NOT `validator.ts` — 06-01-PLAN.md notes `validator.ts` doesn't exist in this tree, canonical_refs in 06-CONTEXT.md is stale on this point). 8 baseline tests (4 checks × valid/violation) + replay-attack, timestamp-spoofing, claimed-impossible-WPM cases. Also updates `README.md` and `scripts/smoke-test.sh`. Depends on 06-01, 06-02, 06-04 — do last.
</remaining_work>

<decisions_made>

- Delegated phase 06 execution to agy/Gemini instead of Claude's native `gsd-executor` wave orchestration, per explicit user request via `/antigravity:delegate gsd-execute-phase 06`.
- Created dedicated branch `phase-06-deploy-hardening` before delegating (write task, per antigravity cost-discipline rules).
- Used `--yolo` (not a narrower `write_file` permissions.allow entry) since the task also needed to run `bun test` (terminal tool).
</decisions_made>

<blockers>
- agy-job timed out mid-run on the whole-phase batch (see BLOCKING CONSTRAINTS above). No human action needed — retry with per-plan delegation.
</blockers>

## Required Reading (in order)
1. `.planning/phases/06-deploy-hardening/06-CONTEXT.md` — decisions D-01..D-08, canonical refs (note the `validator.ts` vs `validate-keystroke.ts` correction called out in 06-01-PLAN.md's own notes).
2. `.planning/phases/06-deploy-hardening/06-01-PLAN.md` — must_haves/prohibitions to verify the already-written code against before committing.
3. `git diff` on this branch — the actual unreviewed changes.

## Infrastructure State
- Branch: `phase-06-deploy-hardening`, checked out, not pushed.
- No servers/watchers running as part of this work.
- agy background job `25690908-023931-329843-23093` is terminal (failed) — nothing to cancel. Log at `~/.antigravity-jobs/25690908-023931-329843-23093/{out,err,rc}`.

<context>
Plan was to have agy implement all 4 plans in wave order (1: 06-01+06-02 parallel, 2: 06-04, 3: 06-03) inside one background job, each ending in its own atomic commit, then verify with `bun test` + `git diff` review afterward. The job ran out before finishing even plan 1's commit. Nothing was verified yet — the diff might be good or might have gaps against the must_haves list, unknown until reviewed.
</context>

<next_action>
Start with: `git diff` on `phase-06-deploy-hardening` to review 06-01's changes against `06-01-PLAN.md`'s must_haves/prohibitions, then run `bun test` in `apps/engine` and `apps/gateway`. Fix and commit 06-01 before touching 06-02/06-04/06-03. Delegate remaining plans one at a time if still using agy.
</next_action>
