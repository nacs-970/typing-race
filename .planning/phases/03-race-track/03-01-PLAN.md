---
phase: 03-race-track
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/passages.ts
  - packages/shared/src/index.ts
  - packages/shared/src/__tests__/passages.test.ts
  - apps/server/src/race/corpus.ts
  - apps/server/src/__tests__/corpus.test.ts
  - apps/server/src/rooms/manager.ts
  - packages/shared/src/messages.ts
  - packages/shared/src/__tests__/messages.test.ts
autonomous: true
requirements: [REQ-10]
user_setup: []

estimate:
  tokens: 48000
  raw_tokens: 32000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "PASSAGES const has 50-100 entries, each 30-60 words, all public-domain English"
    - "getPassageById() and isValidPassageId() resolve any corpus entry"
    - "dealNextPassage() walks a shuffled deck; reshuffles on exhaustion; never serves lastPassageId twice in a row"
    - "createRoom() initializes a fresh shuffled deck + empty usedPassageIds set"
    - "startRaceSchema accepts passageId (UUID v4) + graceSeconds (3/5/10, default 5)"
    - "lobbyStateSchema gains hostPickedPassagePreview (first 30 chars + …) — NEVER the full passageText"
    - "joinedRoomSchema carries hostPickedPassagePreview to non-host joiners; race_start carries full passageText as before"
  artifacts:
    - packages/shared/src/passages.ts (NEW — corpus const + helpers)
    - apps/server/src/race/corpus.ts (NEW — Fisher-Yates dealNextPassage)
    - packages/shared/src/__tests__/passages.test.ts (NEW — corpus shape)
    - apps/server/src/__tests__/corpus.test.ts (NEW — deck shuffle + no-repeat + reshuffle)
    - packages/shared/src/messages.ts (extended startRaceSchema + lobbyStateSchema + joinedRoomSchema)
  key_links:
    - "createRoom() in apps/server/src/rooms/manager.ts initializes room.deckOrder = shuffle(all) and room.usedPassageIds = new Set() and room.lastPassageId = null"
    - "dealNextPassage() in apps/server/src/race/corpus.ts is pure: takes used/deck state, returns next passageId + new deck state"
---

<objective>
Replace the placeholder passage text with a real, curated public-domain corpus. Wire host-side passage selection so the host picks the passage per race; non-hosts see only a 30-char preview until `race_start`. Server initializes a no-repeat shuffled deck on room creation and reshuffles when exhausted without ever serving the same passage twice in a row.

Purpose: REQ-10 (bundled passage corpus) + D-01/D-02 (host picks) + D-03 (public-domain source) + D-04 (no-repeat within room session). Plan 02 (char-state) and Plan 03 (WPM) consume the resolved `passageText` from this plan; Plan 04 (race-end) consumes the `graceSeconds` field this plan introduces.

Output: New `packages/shared/src/passages.ts` (~50 entries + helpers), new `apps/server/src/race/corpus.ts` (Fisher-Yates deck), extended `messages.ts` (startRace gains `passageId`+`graceSeconds`; lobbyState + joinedRoom gain `hostPickedPassagePreview`), initialized deck on `createRoom`.
</objective>

<execution_context>
@/home/nacs/Documents/git/typing-race/.hermes/gsd-core/workflows/execute-plan.md
@/home/nacs/Documents/git/typing-race/.hermes/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-race-track/03-CONTEXT.md
@.planning/phases/03-race-track/03-RESEARCH.md
@.planning/phases/03-race-track/03-VALIDATION.md
@.planning/phases/02-race-engine/02-01-SUMMARY.md
@packages/shared/src/messages.ts
@packages/shared/src/race.ts
@apps/server/src/rooms/manager.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end corpus resolution + host-pick wire extension</name>
  <files>packages/shared/src/passages.ts, packages/shared/src/__tests__/passages.test.ts, packages/shared/src/messages.ts, packages/shared/src/__tests__/messages.test.ts</files>
  <action>
Tracer: create the corpus, the lookup helpers, extend `startRaceSchema` to accept `passageId` + `graceSeconds`, extend `lobbyStateSchema` and `joinedRoomSchema` to carry `hostPickedPassagePreview`, and prove the round-trip with a minimal test.

Per D-03 (corpus = hand-curated public-domain classic literature, ~50 passages, 30-60 words each, const array — no runtime fetch):

1. Create `packages/shared/src/passages.ts`:
   - Export `Passage` interface: `{ id: string; text: string; source: string }` (UUID v4 string, plain text 30-60 words, attribution like "Mark Twain — Tom Sawyer, Ch. 1").
   - Export `PASSAGES: ReadonlyArray<Passage>` — a hand-curated list of AT LEAST 50 entries drawn from public-domain sources (Twain, Carroll, Aesop, Dickens, Shakespeare sonnets, etc.). Each entry's text must be 30-60 words (count by `text.trim().split(/\s+/).length`); reject any entry that falls outside the range before committing.
   - All `id` values must be RFC 4122 v4-conformant UUIDs (group3 starts with `4`, group4 with `8-b`) — same constraint Zod 4 enforces per Phase 2 Plan 01. Use a stable hand-generated range (e.g., `11111111-1111-4111-8111-000000000001` … `11111111-1111-4111-8111-000000000050`).
   - Export `isValidPassageId(id: string): boolean` — uses `PASSAGES.some((p) => p.id === id)`.
   - Export `getPassageById(id: string): Passage | null` — uses `PASSAGES.find((p) => p.id === id) ?? null`.
   - Export `hostPickedPreview(passage: Passage): string` — returns first 30 chars + "…" if `passage.text.length > 30`, else `passage.text`. Truncate on character boundary (not mid-codepoint).

2. Extend `packages/shared/src/index.ts` — re-export `./passages.ts`.

3. Extend `packages/shared/src/messages.ts`:
   - `startRaceSchema` — add `passageId: z.string().uuid()` and `graceSeconds: z.number().int().min(3).max(10).default(5)`. Keep all other fields (currently empty — type + nothing). Re-export the inferred type.
   - `lobbyStateSchema` — add `hostPickedPassagePreview: z.string().optional()` (D-02: non-hosts see only the preview).
   - `joinedRoomSchema` — add `hostPickedPassagePreview: z.string().optional()` (joiners need it too).
   - DO NOT add `passageText` to lobbyStateSchema or joinedRoomSchema — D-02 explicitly locks this; premature full-text leak is Pitfall 4 in RESEARCH.md.

4. Create `packages/shared/src/__tests__/passages.test.ts` with 6+ tests:
   - Test 1: `PASSAGES.length >= 50`.
   - Test 2: every entry has 30-60 words in `text` (count by `trim().split(/\s+/)`).
   - Test 3: every entry has a non-empty `source` string.
   - Test 4: every `id` is unique (Set size === length).
   - Test 5: every `id` parses via `z.string().uuid().safeParse(id).success` (RFC 4122 v4 check).
   - Test 6: `getPassageById(PASSAGES[0].id).text === PASSAGES[0].text`; unknown id returns null.
   - Test 7: `hostPickedPreview(PASSAGES[0])` returns first 30 chars + "…" for long passages; full text for short passages (<=30 chars).
   - Test 8: `isValidPassageId(PASSAGES[0].id) === true`; unknown id returns false.

5. Extend `packages/shared/src/__tests__/messages.test.ts` with 3 round-trip tests for the new schema fields:
   - Test 9: `startRaceSchema.safeParse({ type: "start_race", passageId: PASSAGES[0].id, graceSeconds: 5 }).success`.
   - Test 10: `startRaceSchema.safeParse({ ..., graceSeconds: 99 }).success === false` (out-of-range).
   - Test 11: `lobbyStateSchema.safeParse({ ..., hostPickedPassagePreview: "abc…" }).success`.

The tracer goal: prove the corpus round-trips through Zod, the lookup helpers resolve any entry, and the wire schema accepts host-pick without breaking Phase 2 backwards compat (start_race with no fields should still fail validation since passageId is now required).

Verify: `bun test packages/shared` exits 0; all 8+8 tests pass.
  </action>
  <verify>
  <automated>bun test packages/shared</automated>
  </verify>
  <done>
PASSAGES const has 50+ entries (30-60 words each), startRaceSchema accepts passageId+graceSeconds, lobbyStateSchema + joinedRoomSchema carry hostPickedPassagePreview, all tests green. Tracer commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Server-side no-repeat deck (Fisher-Yates with reshuffle exclusion)</name>
  <files>apps/server/src/race/corpus.ts, apps/server/src/__tests__/corpus.test.ts, apps/server/src/rooms/manager.ts</files>
  <action>
Per D-04 (no-repeat within room session, Fisher-Yates deck, reshuffle on exhaustion with prior-passage exclusion per Pitfall 5):

1. Create `apps/server/src/race/corpus.ts`:
   - Export pure function `shuffle<T>(arr: ReadonlyArray<T>): T[]` — Fisher-Yates; returns a NEW array, does NOT mutate input. Use `Math.random()` (A4 in RESEARCH: not crypto-strong, sufficient for demo).
   - Export pure function `dealNextPassage(args: { allPassageIds: ReadonlyArray<string>; deckOrder: ReadonlyArray<string>; deckCursor: number; lastPassageId: string | null; }): { passageId: string; deckOrder: ReadonlyArray<string>; deckCursor: number }`.
     - If `deckCursor >= deckOrder.length`: reshuffle `allPassageIds.filter(id => id !== lastPassageId)` and prepend nothing — cursor resets to 0. The filter prevents the just-served passage from being the next first-card after reshuffle (Pitfall 5).
     - Else: return `deckOrder[deckCursor]` and increment cursor.
     - Pure: does NOT touch the `room` object; caller is responsible for applying the result.

2. Initialize the deck in `apps/server/src/rooms/manager.ts` `createRoom()`:
   - Add three new fields to the Room construction: `graceSeconds: 5`, `lastPassageId: null`, and the deck state. NOTE: these fields are NOT yet declared on `Room` in `apps/server/src/race/types.ts`. Plan 04 will declare the final Phase 3 Room fields; this plan declares the corpus-specific subset via a minimal local augmentation that Plan 04 unifies.
   - For this plan, keep the manager change minimal: initialize `graceSeconds: 5` on the room literal; do NOT add deck fields yet (Plan 04 owns the full Room extension to avoid two plans editing types.ts).
   - The deck initialization will happen at `start_race` time (in Plan 04's dispatch extension) — Plan 01 wires the helpers only.

3. Create `apps/server/src/__tests__/corpus.test.ts` with 8+ tests:
   - Test 1: `shuffle([1,2,3,4,5])` returns a permutation of length 5 (every element present).
   - Test 2: `shuffle` does not mutate the input array (input === input after call).
   - Test 3: `dealNextPassage({ deckOrder: ["a","b","c"], deckCursor: 0, ... })` returns `{ passageId: "a", deckCursor: 1 }`.
   - Test 4: After exhausting the deck (`deckCursor >= deckOrder.length`), reshuffle happens and `lastPassageId` is excluded from the new deck (run 100 trials to be safe — filter check).
   - Test 5: Sequential deals across 5×N where N = allPassageIds.length never produce the same `passageId` twice in a row when `lastPassageId` is correctly threaded through.
   - Test 6: `dealNextPassage` is pure — calling twice with the same args produces the same output (deterministic w.r.t. inputs; reshuffle randomness is excluded when `deckCursor < deckOrder.length`).
   - Test 7: All-passage coverage — across one full deck cycle (N deals), every `allPassageIds` entry appears at least once.
   - Test 8: `lastPassageId === null` (first race) — reshuffle excludes no entries; deck contains all ids.

Verify: `bun test apps/server/src/__tests__/corpus.test.ts` exits 0; `bun run --filter '*' typecheck` exits 0.
  </action>
  <verify>
  <automated>bun test apps/server/src/__tests__/corpus.test.ts && bun run --filter '*' typecheck</automated>
  </verify>
  <done>
dealNextPassage + shuffle are pure, no-repeat logic is correct, deck exhaustion triggers reshuffle that excludes lastPassageId. Tests green; typecheck clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Type augmentation for corpus fields on Room + dispatch host-pick validation</name>
  <files>apps/server/src/race/types.ts, apps/server/src/ws/dispatch.ts, apps/server/src/__tests__/corpus.test.ts</files>
  <action>
Wire the host-pick flow on the server so `start_race` validates `passageId` against the corpus + threads state through `Room`. Per D-01/D-02 the server is the validator of record.

1. Extend `apps/server/src/race/types.ts` `Room` interface — add corpus + grace fields (this is the minimal set Plan 01 owns; Plan 04 will add `firstFinisherId` and `graceEndsAtServerMs`):
   ```ts
   graceSeconds: number;          // D-09 default 5, host-configurable 3/5/10
   lastPassageId: string | null;  // D-04 — last served; reshuffle excludes this
   // deck fields populated at start_race time:
   deckOrder: string[];           // current shuffled deck (PassageIds)
   deckCursor: number;            // next-to-deal index
   usedPassageIds: Set<string>;   // D-04 history within session
   ```
   The deck fields can be `[]` / `0` / `new Set()` at room creation and populated lazily on the first `start_race` (avoids shuffling for rooms that never race).

2. Extend `apps/server/src/rooms/manager.ts` `createRoom()` to initialize the new Room fields (deck starts empty; first start_race shuffles). Update the `fakeRoom` factory in `apps/server/src/__tests__/race-controller.test.ts` and `apps/server/src/__tests__/validate-keystroke.test.ts` so existing tests still typecheck.

3. Extend `apps/server/src/ws/dispatch.ts` `start_race` case — D-01/D-02 wire-up:
   - Validate `isValidPassageId(msg.passageId)` — if false, send `error{INVALID_FRAME, "unknown passageId"}` and return.
   - Validate `msg.graceSeconds` is in [3,10] (Zod already enforces this in startRaceSchema, but double-check defensively).
   - Resolve `getPassageById(msg.passageId)` and stash on room: `room.passageId = passage.id; room.passageText = passage.text; room.graceSeconds = msg.graceSeconds; room.lastPassageId = passage.id; room.usedPassageIds.add(passage.id)`.
   - Compute `hostPickedPreview(passage)` and stash on room: `room.hostPickedPassagePreview = hostPickedPreview(passage)`.
   - Initialize deck on first race: if `room.deckOrder.length === 0`, set `room.deckOrder = shuffle(PASSAGES.map(p => p.id))` and `room.deckCursor = 0`. Otherwise the host is picking explicitly out of order — that's fine; do NOT advance the deck (the explicit pick is its own record). Add a comment documenting this exception.
   - Broadcast `lobby_state` to all room members carrying the new `hostPickedPassagePreview` (D-02).

4. Extend `apps/server/src/ws/dispatch.ts` `join_room` case and `apps/server/src/ws/broadcast.ts` `broadcastJoinedRoom()` — both must include `hostPickedPassagePreview` on the outgoing `joined_room` frame when present (D-02: joiners need to see "Host chose: …").

5. Update `apps/server/src/__tests__/corpus.test.ts` if needed to confirm the dispatcher consumes `dealNextPassage` correctly (or document that Plan 04 owns the auto-deal path and this plan only wires host-pick).

6. Add 2 new tests to `apps/server/src/__tests__/corpus.test.ts` (or new file `apps/server/src/__tests__/start-race-pick.test.ts`):
   - Test 9: dispatch `start_race` with unknown passageId sends `error{INVALID_FRAME}` and does NOT mutate room state.
   - Test 10: dispatch `start_race` with valid passageId sets room.passageId, room.passageText, room.lastPassageId, room.usedPassageIds, and triggers FSM transition to countdown.

Verify: `bun run --filter '*' typecheck && bun run --filter '*' test` exits 0; `bun --filter '@typing-race/web' build` succeeds (shared schema is imported by web — backward-compat check).
  </action>
  <verify>
  <automated>bun run --filter '*' typecheck && bun run --filter '*' test && bun --filter '@typing-race/web' build</automated>
  </verify>
  <done>
Room has the corpus fields; dispatch validates host-pick; rejected passageIds send error; accepted passageIds thread through Room + broadcast. All tests + typecheck + web build green. Wave 1 component 03-01 ready for parallel 03-02 + 03-03.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → WS → dispatch.start_race | Untrusted client sends passageId — must validate against corpus (Pitfall / V5 input validation) |
| Server → corpus const | Server-internal const, no untrusted input |
| lobby_state / joined_room broadcast → non-host browser | Must carry only preview, NEVER full text (Pitfall 4 — prevents the host-pick from being moot) |
| RaceController tick → corpus.dealNextPassage | Server-internal; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | dispatch.start_race passageId | high | mitigate | `isValidPassageId(msg.passageId)` server-side; reject with INVALID_FRAME if not in PASSAGES (V5) |
| T-03-02 | Information Disclosure | lobby_state / joined_room broadcast | medium | mitigate | Schema carries `hostPickedPassagePreview` (30 chars + …), NEVER full `passageText`. Full text only on `race_start` after race begins. Zod schema omits `passageText` on these two frames as a structural guarantee (Pitfall 4) |
| T-03-03 | Tampering | dispatch.start_race graceSeconds | low | mitigate | Zod `z.number().int().min(3).max(10)` schema (V5); out-of-range rejected at parse time before dispatch |
| T-03-04 | Denial of Service | corpus deck exhaustion | low | accept | D-04 reshuffle handles within-session exhaustion; per-IP rate limit is Phase 4's |
| T-03-SC | Tampering | npm/pip/cargo installs | n/a | n/a | No new dependencies in this plan (RESEARCH.md §Standard Stack: no adds) |
</threat_model>

<verification>
- `bun test packages/shared` — passages.test.ts + messages.test.ts (round-trip + corpus shape + UUID format)
- `bun test apps/server/src/__tests__/corpus.test.ts` — shuffle + dealNextPassage + no-repeat + reshuffle
- `bun run --filter '*' typecheck` — schema extensions + dispatch.start_race extension compile clean
- `bun --filter '@typing-race/web' build` — web still builds (backwards compat for shared schemas)
- Manual grep: `grep -n "passageText" packages/shared/src/messages.ts` — confirm NOT present on lobbyStateSchema or joinedRoomSchema
</verification>

<success_criteria>
1. PASSAGES const has 50+ entries, every entry 30-60 words, every `id` is RFC 4122 v4 UUID (D-03/REQ-10)
2. `isValidPassageId` + `getPassageById` + `hostPickedPreview` exported from `@typing-race/shared` (D-03)
3. `startRaceSchema` accepts `passageId` (UUID v4) + `graceSeconds` (3-10, default 5) (D-01/D-09)
4. `lobbyStateSchema` + `joinedRoomSchema` carry `hostPickedPassagePreview`; neither carries `passageText` (D-02 + Pitfall 4)
5. `dealNextPassage` + `shuffle` are pure; reshuffle excludes `lastPassageId` (D-04 + Pitfall 5)
6. `createRoom()` initializes `graceSeconds: 5`; deck fields default empty; first `start_race` triggers shuffle
7. Dispatch `start_race` with unknown passageId sends `error{INVALID_FRAME}` and does NOT mutate room
8. Dispatch `start_race` with valid passageId threads state through Room + triggers FSM transition + broadcasts updated lobby_state
9. All Wave 0 tests green; typecheck clean across all workspaces
</success_criteria>

<output>
Create `.planning/phases/03-race-track/03-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**Types / interfaces / schemas**
- `packages/shared/src/passages.ts` — `Passage` interface (`id: string; text: string; source: string`); `PASSAGES: ReadonlyArray<Passage>` (50-100 entries); `PassageId` already exported from `race.ts` (re-used)
- `packages/shared/src/messages.ts` — extended `startRaceSchema` (gains `passageId: z.string().uuid()`, `graceSeconds: z.number().int().min(3).max(10).default(5)`); extended `lobbyStateSchema` (gains `hostPickedPassagePreview?: string`); extended `joinedRoomSchema` (gains `hostPickedPassagePreview?: string`)
- `apps/server/src/race/types.ts` — `Room` interface gains `graceSeconds: number; lastPassageId: string | null; deckOrder: string[]; deckCursor: number; usedPassageIds: Set<string>`

**Pure functions / helpers**
- `packages/shared/src/passages.ts` — `isValidPassageId(id: string): boolean`; `getPassageById(id: string): Passage | null`; `hostPickedPreview(passage: Passage): string`
- `apps/server/src/race/corpus.ts` — `shuffle<T>(arr: ReadonlyArray<T>): T[]`; `dealNextPassage(args: { allPassageIds: ReadonlyArray<string>; deckOrder: ReadonlyArray<string>; deckCursor: number; lastPassageId: string | null }): { passageId: string; deckOrder: ReadonlyArray<string>; deckCursor: number }`

**Server-side integration**
- `apps/server/src/rooms/manager.ts` — `createRoom()` initializes `graceSeconds: 5`, deck fields default empty
- `apps/server/src/ws/dispatch.ts` — `start_race` case validates `isValidPassageId(msg.passageId)`; threads passage + grace state through Room; shuffles deck on first race; broadcasts updated `lobby_state` with `hostPickedPassagePreview`
- `apps/server/src/ws/broadcast.ts` — `broadcastJoinedRoom()` carries `hostPickedPassagePreview` when present

**Client-visible schema fields**
- `startRaceSchema.passageId`, `startRaceSchema.graceSeconds` (C→S)
- `lobbyStateSchema.hostPickedPassagePreview` (S→C, optional)
- `joinedRoomSchema.hostPickedPassagePreview` (S→C, optional)

**Test artifacts (Wave 0 / Plan 01)**
- `packages/shared/src/__tests__/passages.test.ts` — 8+ tests (corpus size, word count, source attribution, UUID uniqueness, UUID format, lookup, preview, isValid)
- `packages/shared/src/__tests__/messages.test.ts` — 3 added round-trip tests (startRaceSchema with passageId, graceSeconds out-of-range, lobbyStateSchema with preview)
- `apps/server/src/__tests__/corpus.test.ts` — 10 tests (shuffle, deal cursor advance, reshuffle on exhaustion, lastPassageId exclusion, no-repeat threading, purity, coverage, first-race null lastPassageId, dispatch unknown passageId, dispatch valid passageId)