---
phase: 03.1-wire-word-correctness-into-output-frame
plan: 01
subsystem: race-engine
tags: [investigation, gap-closure, override, verification-process]

requires:
  - phase: 03-race-track
    provides: aggregateWordCorrectness/isWordCorrect in apps/engine/src/race/scoring.ts, decision D-13 (03-CONTEXT.md)
provides:
  - Resolution of 03-VERIFICATION.md Gap B as an accepted override, not a code defect
affects: [03-race-track]

actuals:
  tokens: null
  tasks: 1
  commits: 1

tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified: []

key-decisions:
  - "Reverted an initial build attempt (wire-protocol `words` field + RaceView CSS rendering) after finding 03-CONTEXT.md's decision D-13: 'Word-correctness aggregation in server data model (not UI)... UI just renders per-char accents.' Building UI rendering for aggregateWordCorrectness directly contradicted this pre-existing, documented Phase 3 decision."
  - "Concluded no code change is needed: aggregateWordCorrectness/isWordCorrect are unused-but-intentional per D-13, the same accepted-deviation class as Gap A (D-11/D-12). Added a matching override suggestion to 03-VERIFICATION.md for human sign-off."

patterns-established: []

requirements-completed: []

coverage: []

duration: null
completed: 2026-09-17
status: complete
---

# Phase 03.1 Plan 01: Wire word-correctness into output frame Summary

**Investigated 03-VERIFICATION.md's Gap B ("aggregateWordCorrectness never called from production code") and found it was a verifier miss, not a real defect — Phase 3's own decision D-13 explicitly scopes this function to the server data model, "not UI." Reverted an initial build attempt that had wired it into UI rendering before this was found, and recorded a formal override instead.**

## Accomplishments
- Traced the original gap through `03-CONTEXT.md` and found decision D-13, made during Phase 3's pre-planning discussion, which the original 03-VERIFICATION.md pass had quoted but under-weighted.
- Reverted the wire-protocol (`cursor_update`/`rejoined_room` `words` field) and UI-rendering (`RaceView.tsx` CSS classes, new `word-correctness.ts` helper) work from an earlier attempt at this phase, while preserving the unrelated `ROOM_CLOSED` error-code addition that shared `messages.ts` (belongs to phase 04.1).
- Added a second override suggestion to `03-VERIFICATION.md` (mirroring Gap A's D-11/D-12 pattern), explaining why `aggregateWordCorrectness`/`isWordCorrect` remaining unused is the correct, intentional resting state.
- Full verify chain green after the revert: typecheck clean (4 workspaces), 152/152 backend tests, 84/84 web tests.

## Task Commits

1. **Revert 03.1's UI-rendering attempt after finding D-13; record override** - (this commit)

## Files Created/Modified
- `packages/shared/src/messages.ts` - removed the `words` field from `cursorUpdateSchema`/`rejoinedRoomSchema` (kept the unrelated `ROOM_CLOSED` enum entry)
- `packages/shared/src/__tests__/messages.test.ts`, `apps/engine/src/race/frames.ts`, `apps/engine/src/engine.ts`, `apps/engine/src/__tests__/frames.test.ts`, `apps/web/src/store/race.ts`, `apps/web/src/net/race-client.ts`, `apps/web/src/components/RaceView.tsx`, `apps/web/src/styles.css`, `apps/web/src/__tests__/race-client.test.ts` - reverted to pre-03.1 baseline
- `apps/web/src/core/word-correctness.ts` - deleted (uncommitted at time of revert)
- `.planning/phases/03-race-track/03-VERIFICATION.md` - added Gap B override suggestion
- `.planning/phases/03.1-wire-word-correctness-into-output-frame/03.1-VERIFICATION.md` - rewritten to reflect this outcome (supersedes an earlier `gaps_found` pass against the now-reverted build)

## Decisions Made
- See key-decisions above. Consulted an advisor before committing to the revert, given the sunk cost of already-built, tested, passing code — confirmed D-13 is dispositive and the revert (not a delivery-path fix) is correct.

## Deviations from Plan
- The original plan (per this phase's CONTEXT.md) was to fix a wire-protocol delivery bug in already-built UI-rendering code. That plan was abandoned once D-13 showed the UI-rendering premise itself was wrong, not just its delivery mechanism.

## Issues Encountered
- An earlier attempt at this phase delegated implementation to Antigravity (agy/Gemini) concurrently with sibling phases 04.1/05.1/06.1 in a shared, non-worktree-isolated working tree; agy's self-reports in that attempt were unreliable (cross-attributed sibling phases' work to itself). That attempt also missed decision D-13 during its own re-verification pass despite reading `03-CONTEXT.md` and partially quoting it — a reminder that quoting a decision is not the same as weighing it against the change being proposed.

## Next Phase Readiness
- No blockers. Gap B is closed via a documented override (pending human sign-off, tracked in `03-VERIFICATION.md`), not a code fix. Ready for milestone close.

---
*Phase: 03.1-wire-word-correctness-into-output-frame*
*Completed: 2026-09-17*
