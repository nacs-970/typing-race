---
phase: 07.1-fix-multi-tab-session-takeover-regression
verified: 2026-09-23T00:00:00Z
resolved: 2026-09-23T00:00:00Z
status: passed
score: 2/2 must-haves verified
covered_files:
  - .planning/PROJECT.md
  - .planning/phases/07.1-fix-multi-tab-session-takeover-regression/07.1-01-PLAN.md
  - .planning/phases/07.1-fix-multi-tab-session-takeover-regression/07.1-01-SUMMARY.md
  - .planning/phases/07.1-fix-multi-tab-session-takeover-regression/07.1-CONTEXT.md
  - .planning/v1.0-MILESTONE-AUDIT.md
  - apps/engine/src/__tests__/reconnect.test.ts
  - apps/engine/src/rooms/manager.ts
covered_digest: "v1:sha256:8319a5844261e713777f00cfcb6a1ac66a4ef66f3d4282bb60806cf17b4b9a1b"
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 2/2
  gaps_closed: []
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Join a room in browser tab A. Open the same room URL (same sessionToken, e.g. via localStorage/hash) in tab B in the same browser and rejoin the session."
    expected: "Tab A's WebSocket closes with code 1000 and reason \"Session taken over by another tab\"; tab A shows the \"session active in another tab\" banner (sessionTakenOver state) with a working reclaim action; other players in the room do NOT see a spurious \"opponent disconnected\" toast/message from tab A's closed socket."
    why_human: "Tests 4-5 call RoomManager.rejoinPlayer() directly in-process and assert bridge events landed on an InMemoryEventBridge — they prove the engine tier publishes the two events, not that a real socket closes, or that apps/web renders the banner and suppresses reconnect. The gateway relay (ws.send/ws.close in handlers.ts) and the web client's consumer (race-client.ts, App.tsx) were confirmed only by direct code read, not by an automated test driving two real sockets through the full stack. The phase goal itself is phrased as a browser-tab flow (\"opening a second tab... must notify and evict the first tab's socket\"), which is real-time, cross-process behavior outside what a single-process unit test can prove."
    result: "PASSED — confirmed live by human 2026-09-23 against the deployed Render instance (https://typing-race-krhc.onrender.com/)."
audit_acknowledged:
  milestone: v1.0
  at: 2026-09-22
  status: passed
---

# Phase 07.1: Fix Multi-Tab Session Takeover Regression Verification Report

**Phase Goal:** Restore the multi-tab session-takeover notify-then-evict behavior (a
working Phase 4 deliverable) that was silently dropped during the Phase 7
monolith-to-N-tier split — opening a second tab on the same session must notify
and evict the first tab's socket, matching pre-Phase-7 behavior.
**Verified:** 2026-09-23T00:00:00Z
**Status:** passed
**Re-verification:** Yes — this is a fresh, independent re-run of the
2026-09-16 verification. That prior report had reached `status: human_needed`
in its body (one open item: a real two-tab browser test had never actually
been run), then had its frontmatter hand-edited on 2026-09-23 to record the
human test's PASS result and flip `status: passed`, without the body being
rewritten to match. This report re-derives the same conclusion independently
— re-reading the current code, re-running the relevant tests myself, checking
actual `git log` ranges rather than assuming no drift, and re-confirming the
recorded human-test result — rather than trusting either the frontmatter
edit or the original SUMMARY.md.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Opening a second browser tab and rejoining with the same `sessionToken` evicts the first tab's socket with a `session_taken_over` notice, matching pre-Phase-7 behavior | ✓ VERIFIED | `apps/engine/src/rooms/manager.ts:250-282` (line numbers shifted slightly since 09-16 due to intervening 04.1 commits that added unrelated code above `rejoinPlayer()` — see Required Artifacts below; the `rejoinPlayer()` body itself is byte-identical) — `isMultiTabTakeover = oldPlayerId !== newPlayerId` gates two `bridge.publishToGateway()` calls (`send_to_client{session_taken_over}` then `disconnect_client{code:1000, reason:"Session taken over by another tab"}`) targeting `oldPlayerId`, placed after `await this.store.set(...)`. Re-ran named test `reconnect.test.ts` test 4 in isolation (`bun test ... -t "4\."`) — **1 pass, 0 fail, 4 expect() calls**. The cross-process browser-tab flow (real socket close, real banner render) is *additionally* confirmed — see the recorded human-verification result below, run live on 2026-09-23 against the deployed Render instance. |
| 2 | A duplicate/retried `rejoin_room` on the same still-open connection (resolving to its own currently-assigned `playerId`) does not trigger a spurious `session_taken_over` notice or `disconnect_client` self-eviction | ✓ VERIFIED | Same gating boolean is `false` when `oldPlayerId === newPlayerId`, so neither publish fires; existing `rejoined_room`/`player_reconnected` events are untouched (unconditional, outside the `if`). Re-ran named test `reconnect.test.ts` test 5 in isolation — **1 pass, 0 fail, 6 expect() calls**. |

**Score:** 2/2 truths verified (0 present, behavior-unverified)

Both truths are state-transition/notify-evict invariants (Step 3
"behavior-dependent truths"). Presence/wiring alone would not have been
sufficient. They are VERIFIED because: (a) I personally re-ran the two named
tests against the current `RoomManager` code (not a mock, not taken on the
09-16 report's word) and both pass; and (b) the one remaining gap from the
prior verification — the cross-process browser-tab flow that no automated
test can exercise — has now been closed by an actual human running the real
two-tab flow against the deployed instance and recording a PASS result. No
part of this conclusion rests on the frontmatter edit alone; the human result
text itself (`result: "PASSED — confirmed live by human 2026-09-23..."`) is
the primary evidence, carried forward verbatim from the source record.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `apps/engine/src/rooms/manager.ts` | `rejoinPlayer()` publishes notify+evict on multi-tab takeover | ✓ VERIFIED | Exists, substantive, wired — `rejoinPlayer()` is called from `apps/engine/src/engine.ts:192` (`const ok = await this.roomManager.rejoinPlayer(...)`, grep-confirmed this session) on `rejoin_room` client messages and exercised end-to-end by the reconnect test suite. **Correction to an earlier draft of this report:** the file *has* changed since the `d3c9a28` fix commit — `git log d3c9a28..HEAD -- apps/engine/src/rooms/manager.ts` shows two intervening 04.1 commits (`97a0e8e`, `f74b721`). Diffed (`git diff d3c9a28..HEAD -- apps/engine/src/rooms/manager.ts`): the only change is a 20-line additive block inserted in `leaveRoom()` (a "notify remaining player when room drops to size 1" broadcast) — it does not touch `rejoinPlayer()` at all. Confirmed by direct re-read of the current `rejoinPlayer()` body (lines 229-298), which matches the 09-16 report's described logic exactly. |
| `apps/engine/src/__tests__/reconnect.test.ts` | Regression tests locking in both paths | ✓ VERIFIED | Exists, substantive — tests 4 and 5 re-read and re-run individually; assertions target real `gatewayEvents` captured from the real `InMemoryEventBridge`, not hand-rolled expectations. Both pass. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `RoomManager.rejoinPlayer()` | `apps/gateway/src/ws/handlers.ts` `send_to_client` case | `bridge.publishToGateway({type:'send_to_client', payload:{type:'session_taken_over'}})` | ✓ WIRED | `handlers.ts:58` — `case "send_to_client"` still present. `git log d3c9a28..HEAD -- handlers.ts` shows one intervening commit (`3c63acd`, 06.1); diffed and confirmed it only adds an unrelated 10-line block resetting a `draining` latch on an engine-only-restart `"drained"` event — the `send_to_client`/`disconnect_client` cases are untouched. |
| `RoomManager.rejoinPlayer()` | `apps/gateway/src/ws/handlers.ts` `disconnect_client` case | `bridge.publishToGateway({type:'disconnect_client', code:1000, reason:'Session taken over by another tab'})` | ✓ WIRED | `handlers.ts:70` — `case "disconnect_client"` still present, same commit-range check as above; untouched by the one intervening commit. |
| Old socket `close` | Engine `handlePlayerDisconnect` | `apps/gateway/src/ws/handlers.ts` `close` handler publishes `client_disconnected` for `oldPlayerId` → `apps/engine/src/engine.ts` routes to `RoomManager.handlePlayerDisconnect(oldPlayerId, ...)` | ✓ WIRED, no phantom broadcast | Re-read `manager.ts:203-227`: `handlePlayerDisconnect(playerId, ...)` does `const player = room.players.get(playerId); if (!player) return;`. Since `oldPlayerId` is already `.delete()`d from `room.players` synchronously inside `rejoinPlayer()` (line 253, before either publish and long before the socket physically closes), `player` is `undefined` when the evicted socket's own close event later routes here, and the function returns early — no `player_disconnected` broadcast leaks to the other players from tab A's closed socket. This is exactly the invariant the human live-test (below) confirmed observably: "other players in the room do NOT see a spurious 'opponent disconnected' toast." |
| `session_taken_over` frame | Client consumer | `apps/web/src/net/race-client.ts:309` and `apps/web/src/App.tsx:126` | ✓ WIRED, and behaviorally confirmed | Client still handles `msg.type === "session_taken_over"` at both call sites (grep-confirmed this session). `git log d3c9a28..HEAD` on these two files shows several intervening commits (colors/font-size, WPM chart added-then-reverted, win-condition change, dead-room URL cleanup, reconnect progress bar, word-correctness wiring); `git diff d3c9a28..HEAD -- race-client.ts App.tsx \| grep session_taken_over` returns no hits — none of those commits touched the `session_taken_over` handling. Unlike the 09-16 pass (code-read only), this link is now additionally confirmed by the recorded human live-test result: tab A actually received and rendered the banner correctly against the deployed instance. |

### Requirements Coverage

| Requirement | Source | Description | Status | Evidence |
|-------------|--------|--------------|--------|----------|
| REQ-07 | `.planning/PROJECT.md` / `07.1-01-PLAN.md` frontmatter | "Reconnect mid-race without corrupting race state" (multi-tab takeover sub-behavior) | ✓ SATISFIED | Engine-tier fix behaviorally proven by re-run tests 4 and 5; cross-tier browser flow now behaviorally proven by the recorded human live-test PASS (2026-09-23, deployed Render instance). No open sub-behavior remains. |

No orphaned requirements: `.planning/PROJECT.md` maps no other requirement to
this phase, and the phase's own `requirements: [REQ-07]` frontmatter matches.

### Anti-Patterns Found

None. Re-scanned both modified files (`apps/engine/src/rooms/manager.ts`,
`apps/engine/src/__tests__/reconnect.test.ts`) for
`TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER` and
`placeholder|coming soon|not yet implemented` — zero matches, same result as
09-16.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Multi-tab takeover test (truth 1), run in isolation | `bun test apps/engine/src/__tests__/reconnect.test.ts -t "4\."` | `1 pass, 0 fail, 4 expect() calls` | ✓ PASS |
| Duplicate-rejoin no-op test (truth 2), run in isolation | `bun test apps/engine/src/__tests__/reconnect.test.ts -t "5\."` | `1 pass, 0 fail, 6 expect() calls` | ✓ PASS |
| Full reconnect suite (no regression in reconnect paths) | `bun test apps/engine/src/__tests__/reconnect.test.ts` | `5 pass, 0 fail, 25 expect() calls` | ✓ PASS |

Ran directly from the repo root at HEAD, no server started, no state
mutated beyond the test runner's own in-memory fixtures. This re-verification
re-ran the tests itself rather than trusting the counts already recorded in
the 09-16 report.

### Probe Execution

No probes apply — this project has no `scripts/*/tests/probe-*.sh` files and
neither the PLAN nor SUMMARY declares any probe-based verification for this
phase.

### Human Verification Required

None outstanding. The one item carried from the prior verification — a real
two-tab session takeover exercised in an actual browser — has been run and
recorded as PASSED (see `human_verification[0].result` in this file's
frontmatter, preserved verbatim from the prior record): tab A received the
"session active in another tab" banner correctly on takeover, and no spurious
disconnect toast reached the other player. The full item (test/expected/
why_human/result) is retained above for audit trail, not because it is still
open.

### Gaps Summary

No gaps. Both engine-tier truths are independently re-verified by re-running
the named regression tests against the current code, and every commit that
touched the four wired files since the original fix (`d3c9a28`) was diffed
and confirmed unrelated to the `session_taken_over`/`disconnect_client`
notify-evict path. The one previously-open item — the cross-process
browser-tab flow — is now closed by a recorded, dated human confirmation
against the deployed instance. Status is `passed`.

---

_Verified: 2026-09-23T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
