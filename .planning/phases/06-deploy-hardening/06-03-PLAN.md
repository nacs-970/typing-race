---
phase: 06-deploy-hardening
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - apps/engine/src/__tests__/validate-keystroke.test.ts
  - README.md
  - scripts/smoke-test.sh
autonomous: true
requirements: [REQ-12]

assumption_delta_decision:
  noun: "React Compiler adoption"
  decision: "no-change"
  rationale: "already explicitly decided (D-08: skip, no measured bottleneck) — not an identity-model change, just a config toggle left off"

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  prohibitions:
    - "Do not silently loosen any of the 4 anti-cheat thresholds (50ms pre-start grace, 20ms min-interval, 5-consecutive-error gibberish cap, 50% finish-accuracy floor) to make a demo/smoke-test pass more easily — a failing regression test means the test is wrong or a real bug exists, not that the threshold should move."
  truths:
    - "validateKeystroke rejects a replayed valid keystroke burst no faster than the 20ms floor — replaying the same accepted frame does not let a client type faster than an honest client, and does not spuriously re-trigger finishedAtServerMs (D-07 replay-attack bypass scenario)."
    - "A sustained max-rate valid keystroke stream (each keystroke spaced at exactly MIN_INTERVAL_MS=20ms) produces a currentWpm at or below the structural ceiling implied by the 20ms floor, regardless of the clientTs value the frame carries — proving there is no path to an unbounded/impossible WPM claim (D-07 claimed-impossible-WPM bypass scenario)."
    - "min-interval boundary is inclusive-accept at exactly 20ms: an interval of 19ms since the player's lastKeystrokeAt rejects with RATE_LIMITED, and intervals of 20ms and 21ms both accept (boundary edge case; existing test 3 only covers 5ms/50ms, never the exact boundary)."
    - "README documents fly deploy --strategy immediate (not rolling), graceful shutdown behavior (D-01/D-02/D-03), the current fly.toml kill_timeout=10s vs the 90s drain window mismatch (D-02's explicit caveat), local dev workflow, and the D-08 React Compiler skip rationale — matching ROADMAP Phase 6 criterion #4, documentation only (no fly.toml edits, no live fly deploy — both explicitly out of scope per 06-CONTEXT.md's <domain> boundary)."
    - "scripts/smoke-test.sh boots the pinned-Bun production build locally (bun run build && bun run start in unified mode), verifies GET /health returns 200, and verifies a live WS connection receives a hello frame — runnable without any Fly.io account, card, or CI (the CI gate and live Fly.io deploy are both explicitly deferred per 06-CONTEXT.md)."
  artifacts:
    - apps/engine/src/__tests__/validate-keystroke.test.ts
    - README.md
    - scripts/smoke-test.sh
  key_links:
    - "scripts/smoke-test.sh -> bun run build -> MODE=unified bun run --cwd apps/gateway start -> curl /health -> ws handshake -> teardown"
    - "README's Deploy Strategy section <-> the actual DRAIN_TIMEOUT_MS=90_000 constant and SERVER_SHUTTING_DOWN toast added in 06-01"
---

<objective>
Close out Phase 6 hardening: extend the anti-cheat regression suite with the bypass scenarios D-07 asks for that don't already exist, rewrite the README to document deploy strategy and graceful shutdown, and add a local (non-Fly.io) production smoke test.

Purpose: 06-CONTEXT.md's `<code_context>` claims "no existing test file references anti-cheat — this is a net-new test file." That is factually wrong against the tree: `apps/engine/src/__tests__/validate-keystroke.test.ts` already exists with 22 passing tests covering all 4 checks (state guard, grace, min-interval, char-match), the range check, spoofed-clientTs (test 7), and the Phase 3 char-state/WPM/anti-gibberish/anti-space-drag extensions. D-07's genuinely-missing coverage is narrower: a replay-attack test, a claimed-impossible-WPM test, and the exact 19/20/21ms min-interval boundary. This plan extends the existing file rather than duplicating it.

Flagged assumption (explicit, not silently resolved): ROADMAP.md's Phase 6 success criterion #3 says "WPM cap 250 enforced." No such numeric clamp exists anywhere in the codebase — `computeNetWpm` (apps/engine/src/race/scoring.ts) has no cap, and the only structural rate limit is the 20ms MIN_INTERVAL_MS floor in apps/engine/src/race/validate-keystroke.ts, which implies an asymptotic ceiling around 600 WPM, not 250. No D-NN decision in 06-CONTEXT.md authorizes adding a 250 WPM clamp — D-07 only asks for a "claimed-impossible-WPM" bypass *test*. This plan adds a regression test against the real structural ceiling and surfaces this ROADMAP-vs-implementation mismatch to the user; it does NOT add a 250 WPM clamp (that would be unauthorized new scope, not hardening of an existing decision).

Flagged scope boundary (also explicit): ROADMAP criterion #2 (CI gate) and criterion #5 (live Fly.io deploy + two-browser manual smoke test) are both explicitly deferred per 06-CONTEXT.md's `<domain>` boundary — "fly.toml / actual fly deploy execution / .github/workflows CI gate remain untouched — deferred, not part of this phase's scope." This plan's smoke test is LOCAL-ONLY (build + start + health-check + WS handshake), not a live Fly.io deploy verification; the full two-browser manual race smoke test remains a pre-ship manual step for whoever executes the eventual real deploy.

Output: extended validate-keystroke.test.ts, rewritten README.md, new scripts/smoke-test.sh.
</objective>

<execution_context>
@/home/nacs/.claude/gsd-core/workflows/execute-plan.md
@/home/nacs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-deploy-hardening/06-CONTEXT.md
@.planning/phases/06-deploy-hardening/06-01-PLAN.md
@.planning/phases/06-deploy-hardening/06-02-PLAN.md
@apps/engine/src/race/validate-keystroke.ts
@apps/engine/src/__tests__/validate-keystroke.test.ts
@apps/engine/src/race/scoring.ts
@README.md
@fly.toml
</context>

Note: `scripts/deploy.sh` exists in the working tree (Phase 1 Plan 03 output) but is currently untracked by git (`git status` shows it as `??`) — it is not cited as a context reference above for that reason, but Task 2 references its `WOULD_DEPLOY_CMD` value descriptively since it is real, readable content on disk regardless of tracking state.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Anti-cheat bypass regression tests (replay, impossible-WPM, exact boundary)</name>
  <files>apps/engine/src/__tests__/validate-keystroke.test.ts</files>
  <behavior>
    - Replay attack: accept a valid keystroke at index 0, then replay the identical frame (same index, same char) 3 more times, each spaced at least MIN_INTERVAL_MS (20ms) apart in `now`. Assert `player.progress` never advances past what the first accept produced (replaying index 0 cannot move progress backward or forward), and that `totalKeystrokes` increments once per accepted call (already-existing aggregation behavior, test 11) while `player.currentWpm` does not exceed what a single legitimate accept at that elapsed time would produce — proving replay cannot be used to inflate WPM.
    - Replay attack, sub-boundary: replay the identical frame at less than 20ms since the last accept — assert it is rejected `RATE_LIMITED`, same as any other too-fast keystroke (proves replay gets no special-cased bypass of check 3).
    - Claimed-impossible-WPM: starting from `room.startsAtServerMs = 0`, feed a sustained sequence of valid, always-advancing keystrokes each spaced at exactly `MIN_INTERVAL_MS` (20ms) apart (e.g. 25 keystrokes = 500ms elapsed), each frame's `clientTs` set to a wildly spoofed value (e.g. `now - 600_000`, claiming the keystroke happened 10 minutes earlier than the real `now`). Assert the resulting `player.currentWpm` is (a) computed as if elapsed time were the real `now - startsAtServerMs` (not affected by the spoofed clientTs — same property as existing test 7, now asserted specifically against currentWpm) and (b) does not exceed the structural ceiling implied by `MIN_INTERVAL_MS` for that many correct characters in that elapsed time (i.e. `(correctChars/5) / (elapsedMs/60_000)`, computed independently in the test from the same inputs, not copy-pasted from production code) — proving no combination of spoofed timestamps can make the server report an impossible WPM.
    - Exact min-interval boundary: three variants of the existing "3. min-interval" test using `now - player.lastKeystrokeAt` of exactly 19ms (reject RATE_LIMITED), exactly 20ms (accept), and exactly 21ms (accept) — the existing test only exercises 5ms and 50ms, never the boundary itself.
  </behavior>
  <action>
    Add the four test cases above as new `test(...)` blocks inside the existing `describe("validateKeystroke — 4 anti-cheat checks", ...)` block (boundary cases) and a new `describe("validateKeystroke — D-07 bypass scenarios", ...)` block (replay + impossible-WPM cases) in `apps/engine/src/__tests__/validate-keystroke.test.ts`, reusing the file's existing `fakeRoom`/`fakePlayer`/`fakeFrame` helpers exactly as the 22 existing tests do — do not introduce a second helper set or a new test file. Do not modify `apps/engine/src/race/validate-keystroke.ts` — all 4 checks already behave correctly for every case above; this task is regression coverage only, not a bug fix.
  </action>
  <verify>
    <automated>bun test apps/engine/src/__tests__/validate-keystroke.test.ts</automated>
    <fails_when>non-zero exit code, or "fail" appears in the test summary line, or the output does not contain the string "D-07 bypass scenarios" (the new describe block name, proving the new cases were actually added and ran, not just that the pre-existing 22 passed)</fails_when>
  </verify>
  <done>`apps/engine/src/__tests__/validate-keystroke.test.ts` passes with the 4 new bypass/boundary cases added, and all 22 pre-existing tests still pass unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: README rewrite — deploy strategy, graceful shutdown, restart behavior, React Compiler status</name>
  <files>README.md</files>
  <action>
    Extend `README.md`'s existing `## Deployment` section (keep the current "Multi-Container Topology" and "Single-Container Fallback" subsections as-is) with a new `### Deploy Strategy` subsection documenting: `fly deploy --strategy immediate --remote-only` is the intended command (matches `scripts/deploy.sh`'s existing `WOULD_DEPLOY_CMD` — do not change that script), chosen over a rolling strategy because a single-VM-per-tier deploy has no second instance to roll onto; the actual `fly deploy` invocation and `fly.toml` tuning are explicitly out of scope for Phase 6 (per 06-CONTEXT.md) and remain a follow-up.

    Add a new `### Graceful Shutdown` subsection documenting: on SIGTERM, the process broadcasts a `SERVER_SHUTTING_DOWN` error frame to all connected clients (rendered as a "Server Restarting" toast), stops accepting new `create_room`/`join_room`/`start_race` messages, lets in-flight races finish normally, and exits once drained or after a 90-second hard cap (D-01, D-02) — cite `apps/engine/src/engine.ts`'s `EngineWorker.drain()` and `apps/gateway/src/index.ts`'s `GatewayInstance.drain()` as the implementing code (from 06-01). Explicitly flag: `fly.toml`'s current `kill_timeout` is `"10s"` (read directly from the file), well under the 90s drain window — whoever performs the actual Fly.io deploy work must bump `kill_timeout` to `>= 90s` first, or Fly will SIGKILL mid-drain (D-02's caveat; do not edit `fly.toml` in this task — document only).

    Add a one-line note near the Development section (or wherever React/build tooling is discussed) that React Compiler was evaluated and skipped (D-08) — Phase 5 already isolated cursor rendering outside the React tree via CSS `transform3d`, and no profiling has shown a render bottleneck that would justify it; revisit only if that changes.

    Add a `### Local Smoke Test` subsection referencing the new `scripts/smoke-test.sh` (from Task 3) as the local pre-ship check, and note that the full two-browser manual race verification (join, race, results, rematch) and the CI gate (`bun run start` against pinned Bun before every deploy) are both explicitly deferred out of Phase 6's scope per 06-CONTEXT.md, and remain manual/future work respectively.

    Do not restate secret values anywhere (env var *names* only, e.g. `DRAIN_TIMEOUT_MS`, `REDIS_URL`, `FLY_API_TOKEN` — never a real value).
  </action>
  <verify>
    <automated>grep -c '^### \(Deploy Strategy\|Graceful Shutdown\|Local Smoke Test\)$' README.md</automated>
    <fails_when>grep exits non-zero, or prints a count less than 3 — one or more of the three distinct `###` headings is missing (this counts distinct headings, not incidental mentions of the phrase elsewhere in the doc)</fails_when>
  </verify>
  <done>README.md documents deploy strategy, graceful shutdown, the fly.toml kill_timeout caveat, the D-08 React Compiler skip, and the local smoke test — matching ROADMAP Phase 6 criterion #4.</done>
</task>

<task type="auto">
  <name>Task 3: Local production smoke test script</name>
  <files>scripts/smoke-test.sh</files>
  <action>
    Create `scripts/smoke-test.sh` (executable, `set -euo pipefail`, matching `scripts/deploy.sh`'s existing header/style conventions but performing real actions rather than dry-run pre-flight checks). The script: (1) runs `bun run build` for the client bundle, (2) starts the unified server in the background via `MODE=unified PORT=8080 bun run --cwd apps/gateway start` (or the repo-root equivalent `bun run start`, which already runs the gateway), capturing its PID for teardown, (3) polls `GET http://localhost:8080/health` (via `curl -sf`) up to a short bounded number of retries with a brief sleep between attempts until it returns 200, failing loudly if it never does, (4) opens a WebSocket connection to `ws://localhost:8080/ws` (via a one-line `bun -e` script using the global `WebSocket`, matching the pattern already used in the Dockerfiles' `HEALTHCHECK` lines) and asserts a `hello` frame is received within a short timeout, (5) tears down the background server process (kill by captured PID) regardless of success or failure (`trap ... EXIT`), and (6) prints a final `SMOKE OK` line and exits 0 on full success, or a `SMOKE FAILED` line and non-zero exit on any step's failure. This is the local-only replacement for the deferred CI gate's "smoke `bun run start`" item — it does not touch `fly.toml` or invoke `fly deploy`.
  </action>
  <verify>
    <automated>bash -n scripts/smoke-test.sh</automated>
    <fails_when>non-zero exit code — the script has a bash syntax error</fails_when>
  </verify>
  <done>`scripts/smoke-test.sh` exists, is syntactically valid bash, and running it locally (`./scripts/smoke-test.sh`) builds, boots, health-checks, WS-handshakes, tears down, and prints `SMOKE OK` with exit 0 — a manual pre-ship step documented in Task 2's README addition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Regression test suite -> production `validateKeystroke` | Tests must exercise real attack shapes, not implementation details, or they give false confidence |
| README -> operator running the eventual real deploy | Documentation is the only safeguard once the CI gate is deferred out of scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-08 | Tampering | apps/engine/src/__tests__/validate-keystroke.test.ts | medium | mitigate | New tests exercise `validateKeystroke` through its real exported signature with real millisecond arithmetic and independently-computed expected WPM (not copy-pasted from `computeNetWpm`), so they fail if a future change actually reopens a bypass, not just if internal implementation details shift. |
| T-06-09 | Repudiation | scripts/smoke-test.sh / absence of CI | medium | accept | No CI gate exists this phase (explicitly deferred per 06-CONTEXT.md's `<domain>` boundary) — a future regression could ship without the smoke test being re-run. Accepted because the CI-gate work item is explicitly out of scope for this pass; README documents it as pending follow-up work so it isn't forgotten. |
| T-06-10 | Information Disclosure | README.md | low | mitigate | README documents env var *names* only (`DRAIN_TIMEOUT_MS`, `REDIS_URL`, `FLY_API_TOKEN`, etc.) — never an inline real secret value. |

No new npm/pip/cargo installs in this plan — the package-legitimacy gate is not applicable (no `T-06-SC` row).
</threat_model>

<verification>
- `bun test apps/engine/src/__tests__/validate-keystroke.test.ts` passes with all 22 pre-existing + new bypass/boundary tests green.
- `bash -n scripts/smoke-test.sh` reports no syntax errors.
- `grep -c '^### \(Deploy Strategy\|Graceful Shutdown\|Local Smoke Test\)$' README.md` reports 3.
- `bun run --filter '*' typecheck` and `bun run --filter '*' test` both still pass end-to-end across all three plans (06-01, 06-02, 06-03).
</verification>

<success_criteria>
- D-06/D-07 anti-cheat regression coverage includes the replay, impossible-WPM, and exact-boundary cases that did not already exist.
- README documents deploy strategy, graceful shutdown, restart behavior, and local dev workflow per ROADMAP Phase 6 criterion #4 — scoped to documentation only, per 06-CONTEXT.md's deferred fly.toml/live-deploy/CI-gate boundary.
- A runnable local smoke test exists as the pre-ship check in the absence of the deferred CI gate.
- The ROADMAP-vs-implementation "250 WPM cap" mismatch and the deferred CI-gate/live-deploy criteria (#2, #5) are surfaced explicitly, not silently resolved either way.
</success_criteria>

<output>
Create `.planning/phases/06-deploy-hardening/06-03-SUMMARY.md` when done
</output>
