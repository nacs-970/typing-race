---
phase: 04-reconnect
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - packages/shared/src/messages.ts
  - apps/server/src/race/types.ts
  - apps/server/src/race/validate-keystroke.ts
  - apps/server/src/ws/broadcast.ts
  - apps/server/src/ws/dispatch.ts
  - apps/web/src/net/ws.ts
  - apps/web/src/App.tsx
  - apps/server/src/__tests__/reconnect.test.ts
autonomous: true
requirements: [REQ-07]
user_setup: []

estimate:
  tokens: 46000
  raw_tokens: 31000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "rejoinedRoomSchema defines authoritative race snapshot sent to rejoining client"
    - "rejoinedRoomSchema contains room state, passage text, startsAtServerMs, own progress/charStates, and opponent list"
    - "Server enforces a 500ms grace period after rejoining before accepting new keystrokes"
    - "Client caches sessionToken in sessionStorage under roomCode"
    - "Client auto-rejoins on reconnect using cached sessionToken and hydrates stores from rejoined_room snapshot"
  artifacts:
    - packages/shared/src/messages.ts (rejoinedRoomSchema in serverToClientSchema)
    - apps/server/src/ws/broadcast.ts (buildRejoinedRoomFrame helper)
    - apps/server/src/race/validate-keystroke.ts (500ms anti-cheat reconnect check)
    - apps/web/src/net/ws.ts (sessionToken caching & rejoin logic)
    - apps/web/src/App.tsx (handling rejoined_room hydration)
  key_links:
    - "rejoined_room frame supplies full state so client instantly resumes race without desync"
    - "validateKeystroke rejects frames if now < player.reconnectedAt + 500ms"
---

<objective>
Implement full race state snapshot replay on reconnect (`rejoined_room` S->C frame), client-side store re-hydration, and a 500ms server anti-cheat grace window before reconnected keystrokes are accepted.

Purpose: Fulfill REQ-07 success criterion 2 (server sends full race snapshot on reconnect, 500ms grace before keystrokes count).
</objective>

<tasks>

### Task 1: Wire schema & broadcast helper for `rejoined_room` snapshot
- In `packages/shared/src/messages.ts`:
  - Define `rejoinedRoomSchema`:
    - `type: "rejoined_room"`
    - `roomCode: string`
    - `roomState: raceStateSchema`
    - `passageId: z.string().uuid().nullable()`
    - `passageText: z.string().nullable()`
    - `startsAtServerMs: z.number().nullable()`
    - `graceEndsAtServerMs: z.number().nullable()`
    - `clockOffsetMs: z.number()`
    - `you: object` (playerId, nickname, isHost, progress, charStates, wpm, uncorrectedErrors)
    - `players: array` of player summaries with progress, wpm, and isDisconnected flag
- In `apps/server/src/ws/broadcast.ts`:
  - Implement `buildRejoinedRoomFrame(room: Room, player: Player): RejoinedRoom`.
- In `apps/server/src/ws/dispatch.ts`:
  - When `rejoin_room` succeeds, send `buildRejoinedRoomFrame` to the reconnected socket.

### Task 2: 500ms reconnect anti-cheat grace guard
- In `apps/server/src/race/types.ts`:
  - Add `reconnectedAt: number | null` to `Player` interface.
- In `apps/server/src/rooms/manager.ts`:
  - `rebindPlayerSocket` sets `player.reconnectedAt = Date.now()`.
- In `apps/server/src/race/validate-keystroke.ts`:
  - Add Check 2b: If `player.reconnectedAt !== null && now < player.reconnectedAt + 500`, reject with `RATE_LIMITED`.

### Task 3: Client sessionStorage caching, auto-rejoin & state hydration
- In `apps/web/src/net/ws.ts`:
  - Store `sessionToken` in `sessionStorage.setItem('typing_race_' + roomCode, sessionToken)` upon receiving `joined_room`.
  - When connection reopens, check `sessionStorage` for existing room session; if found, send `rejoin_room`.
  - On receiving `rejoined_room`:
    - Hydrate `useConnectionStore` with `playerId`, `roomCode`.
    - Hydrate `useRaceStore` with `passageText`, `ownCharStates`, `ownWpm`, `countdownStartsAtServerMs`.
    - Hydrate `useCursorStore` with opponent cursors.
</tasks>
