---
phase: 04.1-add-proactive-room-closed-toast
verified: 2026-09-23T17:26:00Z
status: passed
score: 5/5 must-haves verified
covered_files:
  - .planning/ROADMAP.md
  - .planning/phases/04.1-add-proactive-room-closed-toast/04.1-01-SUMMARY.md
  - .planning/phases/04.1-add-proactive-room-closed-toast/04.1-CONTEXT.md
  - apps/engine/src/__tests__/rooms.test.ts
  - apps/engine/src/rooms/manager.ts
  - apps/gateway/src/__tests__/gateway.test.ts
  - apps/web/src/App.tsx
  - packages/shared/src/messages.ts
covered_digest: "v1:sha256:ae0a4bf38d7ad3f3cb100e7f820d64d89d1a072357c115d93f42656cc710ab42"
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: resolved (gaps_found originally, marked resolved in-place on 2026-09-17 without a clean re-verification pass)
  previous_score: 3/5 (pre-fix) — SUMMARY claimed the size===1 retarget closed both gaps but no fresh VERIFICATION pass had confirmed it against current HEAD until now
  gaps_closed:
    - "ROOM_CLOSED broadcast retargeted from size===0 (proven zero real recipients) to size===1 (proven real, reachable recipient) — confirmed still present and unchanged at current HEAD"
    - "Test coverage gap closed: a real two-socket WebSocket integration test (apps/gateway/src/__tests__/gateway.test.ts) now proves actual delivery to the remaining live socket and non-delivery to the leaver's own socket, not just bridge-event emission"
  gaps_remaining: []
  regressions: []
---

# Phase 04.1: Add proactive room-closed toast Verification Report

**Phase Goal:** Close the gap found by 04-VERIFICATION.md: only a reactive "Room Lost" toast existed;
add a genuine proactive push notification within ~5s of the last player leaving a room, delivered to
any other still-connected client viewing that room.
**Verified:** 2026-09-23
**Status:** passed
**Re-verification:** Yes — after gap closure (retarget to size===1 + real two-socket test)

## Context for this pass

The phase's own history: an initial `size===0` broadcast attempt was found, via a prior re-verification
pass, to have zero real recipients ever (an in-memory harness wiring the real `RoomManager` to the real
`ClientManager` proved this across every normal-flow scenario). The fix was retargeted to `size===1` — a
transition proven to have a real, reachable recipient — with corrected toast copy ("Alone in Room" / "All
other players have left this room.") and a real two-socket integration test. This report re-verifies that
retargeted, final state against the current codebase (not the SUMMARY's narrative), and checks whether
any commit since the retarget (`f74b721`) regressed it.

**Note on ROADMAP.md status:** the prior VERIFICATION.md's "ℹ️ Info" about a stale, unfilled ROADMAP.md
Phase 04.1 stub no longer applies — `.planning/ROADMAP.md` lines 127–136 now carry a filled Goal,
Requirements (REQ-09), and a plan checklist entry matching the retargeted implementation (filled by
commit `f74b721`, confirmed by `git show --stat`).

**Note on CONTEXT.md wording vs. implementation:** `04.1-CONTEXT.md`'s success criterion 1 says the
toast fires "once it becomes empty" (i.e., at size===0). The shipped implementation fires at size===1 —
a deliberate, documented scope correction (see `04.1-01-SUMMARY.md` key-decisions and ROADMAP.md's
updated goal text), not a deviation from an unresolved criterion. Truth 1 below is scored against the
corrected, ROADMAP-recorded semantics (a real recipient receives a proactive notification when they
become the last one in the room), not the original CONTEXT wording, per the phase's own recorded
retarget decision.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A proactive notification fires for the one remaining connected client when the second-to-last player leaves/disconnects, without requiring that client to take an action first | ✓ VERIFIED | `apps/engine/src/rooms/manager.ts` `removePlayer()` (lines ~146-163) broadcasts `ROOM_CLOSED` at the `room.players.size === 1` transition. Confirmed reachable via **two** independent departure paths, not just explicit leave: (a) `leave_room` client message → `engine.ts:214` → `removePlayer` directly; (b) silent disconnect (tab closed, no action) → `handlePlayerDisconnect` marks `disconnectedAt`, then `RaceController.tick()` (running on a `setInterval` in `engine.ts:81-82`, iterating **all** rooms via `store.list()`, not just active-race rooms) evicts the player after 60s via `controller.ts:203`'s `await this.roomManager.removePlayer(room.code, player.playerId)` — the same function, same size===1 branch. The recipient never has to act; only the *other* player's departure (by any means) triggers it. |
| 2 | Ordering correctness: the leaver's own (already-untracked) socket never receives its own room's ROOM_CLOSED notice | ✓ VERIFIED | Code structure unchanged from the prior harness-confirmed finding: `player_room_cleared` publishes before the size===1 `broadcast_to_room` publish, synchronously dispatched. Now also backed by a **committed, repeatable assertion** (not just a scratch harness): `apps/gateway/src/__tests__/gateway.test.ts` asserts `client2.messages.some(m => m.type === "error" && m.code === "ROOM_CLOSED") === false` for the departing socket, run and passing (see Behavioral Spot-Checks). |
| 3 | `apps/web/src/App.tsx` renders a toast for the `ROOM_CLOSED` error code, reached via the generic push-capable message handler (not only on the client's own action) | ✓ VERIFIED | Re-traced end-to-end after the intervening `e4493ad` commit (which shifted this code's line numbers 193→201 but did not touch its content): the `msg.code === "ROOM_CLOSED"` branch (line 201) still lives inside the single `ws.subscribe` handler (opened line 108) that processes every inbound server frame; the branch sets `title = "Alone in Room"` / `body = "All other players have left this room."`, which unconditionally reaches `addToast({ type: "error", title, body, durationMs: 5000 })` at the end of the same handler (line ~241). Copy matches the size===1 semantics (not the stale "Room Closed" copy from the original attempt). |
| 4 | Test coverage proves the proactive push actually reaches a live recipient over a real socket, not just that the engine emits an event | ✓ VERIFIED | `apps/gateway/src/__tests__/gateway.test.ts`'s "full multi-client race flow" test drives two real WebSocket clients through create→join→ready→start→keystroke→leave, then asserts client1 (remaining player) receives `{type:"error", code:"ROOM_CLOSED"}` and client2 (leaver) does not. Ran directly: **1 pass, 0 fail, 13 expect() calls**. This is a genuine upgrade from the prior gap: it exercises the real gateway process, real `ClientManager`, and real socket delivery — not a bridge-event-only assertion. Engine-level unit coverage (`rooms.test.ts` #9 and new #9b) additionally confirms the broadcast fires exactly once at size===1 and does not re-fire when the last player subsequently leaves. Ran directly: **10 pass, 0 fail, 30 expect() calls**. |
| 5 | No regression in existing engine/gateway/web test suites | ✓ VERIFIED | Reproduced myself: `bun run typecheck` — clean, 4/4 packages. `bun test packages/shared apps/gateway apps/engine` — 153 pass, 0 fail, 121426 expect() calls, across 23 files. `bun run --cwd apps/web test` (`vitest run`) — one flaky failure on first run in an unrelated test (`SERVER_SHUTTING_DOWN` toast, App.tsx), reproduced clean on two subsequent full/targeted reruns (89/89, then the single named test 2/2) — see note below. No ROOM_CLOSED-related test failed at any point. |

**Score:** 5/5 truths verified (0 present-behavior-unverified, 0 failed)

**Note on the one flaky web-test run:** the first `bun run --cwd apps/web test` invocation in this pass
showed `1 failed | 88 passed` in `SERVER_SHUTTING_DOWN` toast test (App.tsx, unrelated error code path;
the failure message referenced a dynamic-import timing comment about `ws.connect()` being invoked at
module load). Two full/targeted reruns afterward were clean (89/89 full suite; 2/2 on the single named
test via `vitest run -t`). This is flaky test infrastructure (async import timing), not a regression from
this phase's changes — it is not in a file this phase modified, and it does not touch the ROOM_CLOSED
path. Recording it here rather than omitting it.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `apps/engine/src/rooms/manager.ts` | `removePlayer()` broadcasts ROOM_CLOSED at `size===1`, correctly ordered after leaver's own `player_room_cleared` | ✓ VERIFIED | Lines ~134-190; last touched by the retarget commit `f74b721` itself, no later commit modified this file |
| `apps/web/src/App.tsx` | Renders "Alone in Room" toast for `ROOM_CLOSED`, reached via the generic push handler | ✓ VERIFIED | Line 201 (shifted from 193 by an unrelated later commit `e4493ad`, content unchanged — re-traced end-to-end, see truth 3) |
| `packages/shared/src/messages.ts` | `ROOM_CLOSED` present in `errorSchema`'s code enum | ✓ VERIFIED | Line 182, unchanged since prior verification |
| `apps/engine/src/__tests__/rooms.test.ts` | Regression tests proving broadcast fires at size===1 and does not re-fire at size===0 | ✓ VERIFIED | Tests 9 and 9b, both pass |
| `apps/gateway/src/__tests__/gateway.test.ts` | Real two-socket integration test proving live delivery + leaver exclusion | ✓ VERIFIED | Extended "full multi-client race flow" test, passes |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `RoomManager.removePlayer` (size===1 branch) | `apps/gateway/src/ws/handlers.ts` `broadcast_to_room` case | `bridge.publishToGateway({type: "broadcast_to_room", ...})` | ✓ WIRED | Confirmed via passing gateway integration test — real delivery, not just dispatch |
| `broadcast_to_room` handler | `apps/web/src/App.tsx` toast render | `ClientManager.getRoomSockets` → `ws.send` → client `ws.subscribe` handler → `addToast` | ✓ WIRED, DATA FLOWS | Re-traced after intervening commit; branch reaches `addToast` unconditionally |
| `handlePlayerDisconnect` (silent tab-close path) | `RaceController.tick()` 60s grace eviction → `RoomManager.removePlayer` | `setInterval`-driven tick, `store.list()` over all rooms | ✓ WIRED | Confirms the notification also fires for a silent disconnect, not only an explicit `leave_room` — closes the one path the prior pass's harness had not exercised through this specific size===1 branch |
| `player_room_cleared` publish (leaver) | `ClientManager.clearRoom` (ordering, leaver-exclusion) | Synchronous `EventEmitter` dispatch, now also asserted by a committed test | ✓ WIRED, VERIFIED BY TEST | `gateway.test.ts` asserts leaver's socket never sees its own room's ROOM_CLOSED |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Engine-level size===1 broadcast + no re-fire at size===0 | `bun test apps/engine/src/__tests__/rooms.test.ts` | 10 pass, 0 fail, 30 expect() calls | ✓ PASS |
| Real two-socket delivery + leaver exclusion | `bun test apps/gateway/src/__tests__/gateway.test.ts` | 1 pass, 0 fail, 13 expect() calls | ✓ PASS |
| Full backend regression | `bun test packages/shared apps/gateway apps/engine` | 153 pass, 0 fail, 121426 expect() calls (23 files) | ✓ PASS |
| Web regression | `bun run --cwd apps/web test` | 88/89 then 89/89 (reproducibly clean on rerun; one flaky, unrelated failure — see note) | ✓ PASS |
| Typecheck across workspace | `bun run typecheck` | Clean, 4/4 packages | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| REQ-09 | 04.1-01 | Room lifecycle: sweeper, heartbeat, rate limit — this proactive-notification fix is correctly scoped here (relabeled from an initial REQ-07 mislabel per the prior pass's finding) | ✓ SATISFIED | `ROADMAP.md` line 130 now correctly attributes REQ-09; `manager.ts`'s size===1 broadcast + both test suites |

### Anti-Patterns Found

Scanned all `covered_files` for `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER`. One match,
`apps/engine/src/__tests__/rooms.test.ts:44` (`"XXXXXX"`), is test fixture data for an invalid room
code, not a debt marker. No debt-marker gate triggered.

No later commit touching any covered file regressed this behavior:
- `apps/engine/src/rooms/manager.ts` and `apps/gateway/src/__tests__/gateway.test.ts`: last touched by the retarget commit `f74b721` itself — nothing since.
- `apps/web/src/App.tsx`: touched once since (`e4493ad`, player-customizable colors/font size) — confirmed the ROOM_CLOSED branch and its wiring to `addToast` were untouched by that diff (re-traced above).
- `apps/gateway/src/ws/client-manager.ts` (the `roomMembers` 1:1-tracking mechanism the whole fix depends on): last touched by `3c63acd`, well before the retarget — unrelated (drain-latch fix), and predates this phase's fix entirely.
- `packages/shared/src/messages.ts`: unchanged since prior verification.

This phase shows as "stale" in `init.manager` purely because sibling files it covers (`App.tsx`,
`ROADMAP.md`) were touched by later, unrelated commits (colors/fonts feature; roadmap-fill and
audit-closure bookkeeping) — none of which touched the ROOM_CLOSED behavior itself, as confirmed above.

### Gaps Summary

None. Both gaps from the original `gaps_found` pass are closed and remain closed at current HEAD:
1. The broadcast trigger is retargeted to `size===1` (a real, reachable recipient), not `size===0`.
2. A real two-socket WebSocket integration test proves actual delivery and leaver-exclusion, superseding the original bridge-event-only test.

Additionally confirmed in this pass (beyond what the original gap-closure re-verification checked): the
notification also fires correctly when the departure is a silent disconnect (tab closed, 60s grace
eviction) and not only an explicit `leave_room` — both paths converge on the same `removePlayer` size===1
branch. No later commit touching any file this fix depends on altered its behavior.

---

_Verified: 2026-09-23_
_Verifier: Claude (gsd-verifier)_
