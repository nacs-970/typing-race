---
phase: 04-reconnect
plan: 05
type: execute
gap_closure: true
gap_ids: [G-04-1, G-04-2, G-04-3]
files_modified:
  - apps/web/src/components/RaceView.tsx
  - apps/web/src/net/ws.ts
  - apps/web/src/App.tsx
  - apps/server/src/ws/broadcast.ts
  - apps/server/src/rooms/manager.ts
  - apps/server/src/race/controller.ts
autonomous: true
requirements: [REQ-07]
user_setup: []

estimate:
  tokens: 35000
  raw_tokens: 20000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Guest immediately rejoins the race at their exact progress and typed characters with cursor positioned at the latest char typed, not reset to the first char (G-04-1)"
    - "When room returns to lobby or finishes, opening a new tab properly restores the lobby view (or results view if finished), resetting passageText and race view state if in lobby (G-04-2)"
    - "When host disconnects and is evicted after 60s, guest is promoted to host with visible host controls in UI; refreshing does not destroy the room prematurely (G-04-3)"
  artifacts:
    - apps/web/src/components/RaceView.tsx
    - apps/web/src/net/ws.ts
    - apps/web/src/App.tsx
    - apps/server/src/ws/broadcast.ts
    - apps/server/src/rooms/manager.ts
  key_links:
    - "RaceView initializes and preserves ownIndex from useCursorStore on reconnect"
    - "rejoined_room respects roomState and clears passageText in lobby or populates raceEndResults in finished"
    - "App.tsx listens to lobby_state to dynamically update isHost status"
    - "handlePlayerDisconnect in manager.ts grants disconnect grace in lobby so refresh doesn't destroy room"
---

<objective>
Close UAT gaps G-04-1, G-04-2, and G-04-3 discovered during Phase 4 verification:
1. G-04-1: Fix cursor reset to index 0 on reconnect in `RaceView.tsx`.
2. G-04-2: Fix view routing on rejoin into lobby or finished state (avoid stuck 0s race view).
3. G-04-3: Dynamically reflect host promotion in UI on `lobby_state`, and allow disconnect grace in lobby so page refresh doesn't destroy the room.
</objective>

<tasks>

### Task 1: Fix Reconnect Cursor Position Reset (G-04-1)
- In `apps/web/src/components/RaceView.tsx`:
  - Remove `useEffect(() => { updateOwnIndex(0); }, [passageText])` which unconditionally resets `ownIndex` to 0 on mount.
  - Initialize `ownIndex` and `ownIndexRef` from `useCursorStore.getState().ownIndex`.
  - Keep `useEffect` on `storeOwnIndex` so server-restored `ownIndex` sets the local cursor.
  - Reset to 0 only when race actually resets (via `race_end` or `countdown` resetting `useCursorStore`).

### Task 2: Fix Rejoin in Lobby / Finished Room State (G-04-2)
- In `apps/server/src/ws/broadcast.ts`:
  - In `buildRejoinedRoomFrame`, if `room.state === "lobby"`, set `passageText: null` and `passageId: null`.
  - If `room.state === "finished"`, include results snapshot if applicable.
- In `apps/web/src/net/ws.ts`:
  - In `rejoined_room` handler:
    - If `msg.roomState === "lobby"`:
      - Clear `passageText: null`, `countdownStartsAtServerMs: null`, `graceBanner: null`, `raceEndResults: null`.
    - If `msg.roomState === "finished"`:
      - Set `passageText: msg.passageText`.
- In `apps/web/src/App.tsx`:
  - Ensure view rendering correctly prioritizes `roomCode && !raceStart` for `LobbyView`.

### Task 3: Host Promotion in UI & Solo Lobby Disconnect Grace (G-04-3)
- In `apps/web/src/App.tsx`:
  - In `ws.subscribe` listener:
    - Handle `lobby_state`: find local player in `msg.players` matching `playerId` (or `useConnectionStore.getState().playerId`) and call `setIsHost(localPlayer.isHost)`.
    - Handle `player_left`: update host state if needed.
- In `apps/server/src/rooms/manager.ts`:
  - In `handlePlayerDisconnect`:
    - Instead of immediately deleting solo player rooms in lobby, record `player.disconnectedAt = Date.now()` and grant 60s grace.
    - If a player reloads or re-opens, `rejoin_room` reconnects them seamlessly.
    - In `controller.ts` `tick()`, if a player's 60s grace expires, `removePlayer` is called. If all players in the room are gone, `rooms.delete(code)` removes the room.
</tasks>
