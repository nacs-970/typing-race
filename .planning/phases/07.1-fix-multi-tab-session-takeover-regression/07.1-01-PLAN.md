---
phase: 07.1-fix-multi-tab-session-takeover-regression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/engine/src/rooms/manager.ts
  - apps/engine/src/__tests__/reconnect.test.ts
autonomous: true
requirements: [REQ-07]

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "Opening a second browser tab and rejoining with the same sessionToken evicts the first tab's socket with a session_taken_over notice, matching pre-Phase-7 behavior (04-02-PLAN.md deliverable, lost in the Phase 7 N-tier split)"
    - "Refreshing the same tab (rejoin resolves to the SAME playerId) does not trigger a spurious session_taken_over notice or disconnect — no regression on the common reconnect path"
  artifacts:
    - apps/engine/src/rooms/manager.ts
    - apps/engine/src/__tests__/reconnect.test.ts
  key_links:
    - "RoomManager.rejoinPlayer() -> bridge.publishToGateway({type:'send_to_client', payload:{type:'session_taken_over'}}) -> apps/gateway/src/ws/handlers.ts send_to_client case -> old socket receives the frame"
    - "RoomManager.rejoinPlayer() -> bridge.publishToGateway({type:'disconnect_client', code:1000, reason:'Session taken over by another tab'}) -> apps/gateway/src/ws/handlers.ts disconnect_client case -> old socket is ws.close(1000, reason)'d"
---

<objective>
Restore the multi-tab session-takeover notify-then-evict behavior that was a
working Phase 4 deliverable (`04-02-PLAN.md`) and was silently dropped during
the Phase 7 monolith-to-N-tier split. `RoomManager.rejoinPlayer()` currently
reassigns `room.players` from `oldPlayerId` to `newPlayerId` on a multi-tab
rejoin but never notifies or evicts the superseded socket, so the stale tab
sits connected, believing it still owns the player slot.

Purpose: Restore requirement REQ-07 ("Reconnect mid-race without corrupting
race state") to fully-satisfied — this is the multi-tab half of that
requirement, found broken by `/gsd-audit-milestone`
(`.planning/v1.0-MILESTONE-AUDIT.md`, finding REQ-07-multitab). Root cause,
exact fix location, and exact fix mechanism are already fully diagnosed in
`07.1-CONTEXT.md` (D-01 through D-04) — this plan translates those decisions
directly into code, no new design work.

Output: `rejoinPlayer()` publishes the two missing bridge events on a
genuine multi-tab takeover; a regression test locks in both the fixed path
and the still-correct same-tab path.
</objective>

<execution_context>
@/home/nacs/.claude/gsd-core/workflows/execute-plan.md
@/home/nacs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-fix-multi-tab-session-takeover-regression/07.1-CONTEXT.md

@apps/engine/src/rooms/manager.ts
@apps/engine/src/__tests__/reconnect.test.ts
@apps/engine/src/race/frames.ts
@packages/shared/src/bridge.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Restore session_taken_over notify + evict on multi-tab rejoin</name>
  <files>apps/engine/src/rooms/manager.ts, apps/engine/src/__tests__/reconnect.test.ts</files>
  <behavior>
    - Test A (multi-tab): given a room with a connected player, calling
      `rejoinPlayer(code, sessionToken, newPlayerId)` where `newPlayerId !==`
      the player's current id (`oldPlayerId`) causes the captured
      `gatewayEvents` array to contain exactly one event matching
      `{type:"send_to_client", playerId: oldPlayerId, payload:{type:"session_taken_over"}}`
      and exactly one event matching
      `{type:"disconnect_client", playerId: oldPlayerId, code:1000, reason:"Session taken over by another tab"}`.
      Additionally, at the moment the `disconnect_client` event is observed
      by a listener, `room.players.has(oldPlayerId)` is already `false` —
      the map reassignment must be visible before the old socket is told to
      disconnect, so a `client_disconnected` bounce-back from the gateway
      (triggered by that `disconnect_client`) can never resolve `oldPlayerId`
      back to the live, just-reconnected player object.
    - Test B (same-tab, no regression): given the same setup but calling
      `rejoinPlayer(code, sessionToken, sameOldPlayerId)` (identical id —
      simple refresh), `gatewayEvents` contains ZERO events with
      `payload.type === "session_taken_over"` and ZERO `disconnect_client`
      events targeting that player — the existing `rejoined_room` /
      `player_reconnected` events (already covered by
      `reconnect.test.ts` test 3) still fire unchanged.
  </behavior>
  <action>
    First (RED), add both tests above to `apps/engine/src/__tests__/reconnect.test.ts`,
    inside the existing `describe("sessionToken & reconnect handshake")` block,
    mirroring the `gatewayEvents` capture pattern already used in test 3
    (`bridge.onGatewayEvent` push into the module-level `gatewayEvents` array
    set up in `beforeEach`). For Test A's ordering check, register an
    additional, test-scoped `bridge.onGatewayEvent` listener (in addition to
    the shared `beforeEach` one) that, when it observes a `disconnect_client`
    event for the old player id, records whether `room.players.has(oldPlayerId)`
    is `false` at that instant (using the `room` object already returned by
    `manager.createRoom(...)`, same pattern as test 3's `room.players.get(...)`
    checks) — then assert that recorded value is `true` after `rejoinPlayer`
    resolves. Run the tests and confirm Test A fails (no `session_taken_over`/
    `disconnect_client` events exist yet) while Test B passes trivially.

    Then (GREEN), in `apps/engine/src/rooms/manager.ts` `rejoinPlayer()`:
    capture the existing takeover condition once, e.g.
    `const isMultiTabTakeover = oldPlayerId !== newPlayerId;`, at the point
    the current `if (oldPlayerId !== newPlayerId)` check begins (~line 230),
    and reuse that boolean for both (a) the existing `room.players`
    reassignment block (unchanged logic, just gated by the named boolean
    instead of the raw re-typed condition) and (b) the two new bridge
    publishes below — this satisfies CONTEXT.md's discretion note to "reuse
    it, don't duplicate the condition" while still separating the two
    concerns. Add the two new `this.bridge.publishToGateway(...)` calls
    **immediately after the existing `await this.store.set(roomCode, room);`
    call (~line 245) and before the existing `player_room_assigned` publish**
    — NOT before or interleaved with the `room.players` reassignment lines.
    This ordering matters: in split-IPC/Redis bridge mode, publishing
    `disconnect_client` triggers the gateway to close the old socket, whose
    close handler publishes `client_disconnected{playerId: oldPlayerId}`
    back to the engine, which may be read by a different engine instance via
    `this.store.get(roomCode)`. If the reassignment hasn't been persisted via
    `await this.store.set(...)` yet, that read would still show `oldPlayerId`
    in `room.players`, and `handlePlayerDisconnect` would resolve it back to
    the same, just-reconnected player object — setting a bogus
    `disconnectedAt` on a live session. Persisting first (existing code
    already does this before any publish) and only then evicting the old
    socket avoids that race entirely, matching the monolith's equivalent
    guard (`oldWs.data.roomCode = null` before `close()`,
    `apps/server/src/rooms/manager.ts:135-152` at commit `802ba24~1`).

    Publish, in order: first `{type: "send_to_client", playerId: oldPlayerId, payload: {type: "session_taken_over"}}`,
    then `{type: "disconnect_client", playerId: oldPlayerId, code: 1000, reason: "Session taken over by another tab"}`,
    both wrapped in `if (isMultiTabTakeover) { ... }`. Use an inline object
    literal for the `session_taken_over` payload — this matches the existing
    codebase convention for single-field no-arg payloads (e.g.
    `payload: { type: "return_to_lobby" }` in `apps/engine/src/engine.ts`),
    no new frame builder needed. This is the exact message type and close-code
    pair from the pre-Phase-7 implementation, so no other code (client toast
    copy in `apps/web/src/App.tsx`, close-code handling in
    `apps/web/src/net/race-client.ts`) needs to change — both already
    correctly implement the consumer side, confirmed dead-code-until-now by
    CONTEXT.md's grep. Do not modify `apps/gateway/src/ws/handlers.ts` or
    `packages/shared/src/messages.ts` (D-04 — both tiers already correctly
    implement their half of this contract; this is a same-tier, engine-only
    change).

    Run both tests again and confirm both now pass (Test A green, including
    the ordering assertion; Test B still green). Then run the full-suite
    verify below to confirm no regression elsewhere.
  </action>
  <verify>
    <automated>bun test apps/engine/src/__tests__/reconnect.test.ts apps/engine/src/__tests__/rooms.test.ts</automated>
    <fails_when>exit code is non-zero, or output does not contain "0 fail" for these two files — a failed assertion prints "expect(received).toBe(expected)" naming the mismatched or leaked `session_taken_over`/`disconnect_client` event, or `false` for the eviction-before-disconnect ordering check</fails_when>
    <automated>bun run typecheck && bun test packages/shared apps/gateway apps/engine && bun run --cwd apps/web test</automated>
    <fails_when>exit code is non-zero anywhere in the chain — a non-zero `tsc` exit prints type errors to stderr, and a non-zero `bun test` exit prints "fail" counts greater than 0 for at least one of `packages/shared`, `apps/gateway`, `apps/engine`, or `apps/web`</fails_when>
  </verify>
  <done>
    `rejoinPlayer()` publishes `send_to_client{session_taken_over}` then
    `disconnect_client{code:1000, reason:"Session taken over by another tab"}`
    for `oldPlayerId` on every multi-tab rejoin (`oldPlayerId !== newPlayerId`),
    both published only after the reassignment is persisted via
    `await this.store.set(...)`, and publishes neither on a same-tab rejoin.
    Both new tests pass (including the eviction-before-disconnect ordering
    assertion); no existing test in `reconnect.test.ts` or `rooms.test.ts`
    regresses; full-suite verify command passes with zero failures.
  </done>
  <reversibility rating="reversible">Purely additive — two new `bridge.publishToGateway()` calls after an existing persistence point, no signature or schema changes; removing them fully reverts to current (buggy) behavior. Per CONTEXT.md D-02.</reversibility>
  <threat_model>
  ## Trust Boundaries

  | Boundary | Description |
  |----------|-------------|
  | engine tier -> gateway tier | Internal bridge (unified in-process, split-IPC, or Redis pub/sub) carrying `EngineToGatewayEvent`s; not directly reachable by an external client, but a same-process/same-cluster boundary the fix's ordering guarantee depends on |
  | gateway -> client socket | `disconnect_client`/`send_to_client` cross to an untrusted browser peer over the existing WebSocket; the peer receives a forced close it did not request |

  ## STRIDE Threat Register

  | Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
  |-----------|----------|-----------|----------|-------------|-----------------|
  | T-07.1-01 | Information Disclosure | `rejoinPlayer()` event publish | low | accept | `oldPlayerId` used for both new events is the exact id resolved by the pre-existing session-token lookup a few lines above (same value already used for `player_room_assigned`/`send_to_client rejoined_room` in this function) — no new identity-resolution logic, no new spoofing surface |
  | T-07.1-02 | Denial of Service | Bridge transport (unified/split-IPC/Redis modes) | low | accept | If the bridge is down between the two publish calls, the old socket simply isn't notified/evicted — identical to today's pre-fix behavior, not a new failure mode; gateway's `disconnect_client` handler already wraps `ws.close()` in try/catch and no-ops if the socket is already gone |
  | T-07.1-03 | Tampering | Stale socket window between reassignment and eviction | low | accept | The reassignment is persisted via `await this.store.set(...)` BEFORE the eviction publishes fire, so `room.players` is already keyed by `newPlayerId` (in every store backend, including Redis) by the time the stale socket is closed — any message the old socket sends in the gap, or any `client_disconnected` bounce-back its close triggers, is routed against a player id no longer present in `room.players` and rejected/no-ops via existing lookups elsewhere in the engine — no new trust granted to the stale socket |
  </threat_model>
</task>

</tasks>

<verification>
Full-suite regression check (matches this project's established Phase 7
verify pattern — same commands, unchanged by this fix):

```
bun run typecheck && bun test packages/shared apps/gateway apps/engine && bun run --cwd apps/web test
```

Expect 0 typecheck errors and 0 test failures across all suites (baseline
from `07-03-SUMMARY.md`: 122 passed, 0 failed across 20 files in
`packages/shared apps/gateway apps/engine`, plus the two new tests in this
plan).
</verification>

<success_criteria>
- `git show 802ba24~1:apps/server/src/rooms/manager.ts` behavior (notify then
  evict, code 1000, exact reason string) is reproduced in
  `apps/engine/src/rooms/manager.ts` `rejoinPlayer()`
- `bun test apps/engine/src/__tests__/reconnect.test.ts apps/engine/src/__tests__/rooms.test.ts` passes with the two new assertions included
- `bun run typecheck && bun test packages/shared apps/gateway apps/engine && bun run --cwd apps/web test` passes with zero failures (no regression introduced elsewhere)
- No changes to `apps/gateway/src/ws/handlers.ts` or `packages/shared/src/messages.ts` (already correct per D-04)
</success_criteria>

<output>
Create `.planning/phases/07.1-fix-multi-tab-session-takeover-regression/07.1-01-SUMMARY.md` when done
</output>
